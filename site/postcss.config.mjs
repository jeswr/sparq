// Tailwind v4 — the PostCSS plugin is the whole build; the theme lives in CSS
// (src/app/globals.css), there is no tailwind.config.ts (matches solid-pod-manager).
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
