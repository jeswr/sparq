# [OPUS-4.8] sq-6qw3: wasm-pack build artifacts — generated, never committed (mirrors
# crates/sparq-wasm/.gitignore). The bundle is rebuilt from source by CI / the site deploy.
/pkg
/pkg-node
# [FABLE-5] sq-ixc3.20 — packages/eyereasoner-compat's DEDICATED out-dir (built WITHOUT the
# `explain` feature); separate from /pkg so its `prepare`-hook rebuild can never clobber the
# explain-enabled bundle the GUI/site sync from /pkg.
/pkg-eyereasoner-compat
