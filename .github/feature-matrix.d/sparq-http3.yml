# [GPT-5.6] sq-oprna.1: the internal HTTP/3 helper is dependency-free by default.
# This leg activates the default-off server surface and executes its real QUIC/h3
# integration suite, including axum dispatch and peer ConnectInfo injection.
- name: "sparq-http3 (server)"
  crate: "sparq-http3"
  features: "server"
  test: true
