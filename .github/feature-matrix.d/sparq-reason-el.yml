# Feature-matrix fragment for `sparq-reason-el`.
#
# One self-contained leg mapping per opt-in feature(group). Assembled into the
# feature-matrix `opt-in-features` strategy by scripts/assemble-feature-matrix.py
# (the `setup` job emits the combined matrix as JSON consumed via fromJSON). Adding a
# leg for THIS crate edits only THIS file, so concurrent opt-in-feature PRs for
# DIFFERENT crates never textually conflict (bead sq-ibrze). [OPUS-4.8]
#
# CONTRACT (gate-critical): each leg's emitted check-run NAME is `opt-in <name>`,
# discovered as a REQUIRED check by the ci-summary `gate` aggregator BY NAME. Keep
# `name` free of the whole words "advisory"/"informational" or the leg stops gating.
# Fields: name (required -> check-run name), crate (required), features (required,
# non-empty comma list), test (bool — run `cargo test` for the leg).

# [OPUS-4.8] sq-xetf7 (Phase E2): sparq-reason-el's `rbox` feature — RBox / property
# chain + transitive-role saturation (CR10 role inclusion + CR11 role composition).
# The NOTE at the head of this file (sq-evb1) anticipated this leg: E1 ships the
# crate with NO intra-crate feature, but E2 adds the default-OFF `rbox` feature, so
# without a leg here the whole RBox surface goes UNGATED in CI. tests/rbox_differential.rs
# is `#![cfg(feature = "rbox")]` (8 CR10/CR11 closure-oracle tests) and the rbox.rs +
# extract.rs/classify.rs role-automaton arms are all `#[cfg(feature = "rbox")]`, so the
# default build (and ci.yml's `--workspace --all-targets` archive, which passes NO
# `--features`) compiled them EMPTY and ran ZERO of them — the silent gap this leg
# closes. No new dep (`rbox = []`, reuses oxrdf + rustc-hash). Verified locally: `cargo
# nextest list -p sparq-reason-el --features rbox` shows the 8 rbox_differential tests +
# the 4 in-src rbox::tests, and `clippy --all-targets -D warnings` is clean with it on.
- name: "sparq-reason-el (rbox — CR10/CR11 RBox saturation)"
  crate: "sparq-reason-el"
  features: "rbox"
  test: true

# [OPUS-4.8] sq-s2nob (Phase E3): sparq-reason-el's `hasse` feature — transitive
# reduction to the DIRECT-subsumer Hasse diagram (`DirectHierarchy` + `classify_hasse_
# graph`) + equivalence-class collapse + the membership-indexed reduction. Same gap as
# the rbox leg above: tests/hasse_reduction.rs is `#![cfg(feature = "hasse")]` (8 Hasse
# closure-oracle + edge-count tests) and the whole hasse.rs module is `#[cfg(feature =
# "hasse")]`, so the default build (and ci.yml's `--workspace --all-targets` archive,
# which passes NO `--features`) compiles them EMPTY and runs ZERO of them — this leg is
# the ONLY thing that executes them. The `hasse_bench` example is `required-features =
# ["hasse"]` so it compiles here too. No new dep (`hasse = []`, reuses rustc-hash).
# Verified locally: `cargo nextest list -p sparq-reason-el --features hasse` shows the 8
# hasse_reduction tests, and `clippy --all-targets -D warnings` is clean with it on.
- name: "sparq-reason-el (hasse — transitive reduction / direct-subsumer Hasse)"
  crate: "sparq-reason-el"
  features: "hasse"
  test: true

# [FABLE-5] sq-pbz04.2.2: sparq-reason-el's `cdomain` feature — concrete domains
# (CR7–CR9): faceted `owl:onDatatype`/`owl:withRestrictions` satisfiability +
# `DataHasValue`/singleton-`DataOneOf` point ranges on the shared
# `sparq_substrate::numeric` exact tier. Same gap as the rbox/hasse legs above:
# tests/cdomain.rs is `#![cfg(feature = "cdomain")]` (the CR7 unsat / CR8 containment /
# deferral-boundary closure oracles) and the whole cdomain.rs module + the extract.rs
# hooks are `#[cfg(feature = "cdomain")]`, so the default build compiles them EMPTY and
# runs ZERO of them — this leg is the only thing that executes them. New OPTIONAL dep:
# sparq-substrate (numeric slice only), linked ONLY by this feature.
- name: "sparq-reason-el (cdomain — CR7–CR9 concrete domains)"
  crate: "sparq-reason-el"
  features: "cdomain"
  test: true

# [OPUS-4.8] sq-pbz04.2.5: sparq-reason-el's `abox` feature — ABox realisation +
# whole-ontology consistency. Same gap as the rbox/hasse/cdomain legs above: the
# behavioral tests in `src/abox.rs` are gated `#[cfg(feature = "abox")]` (the whole
# module is feature-gated), so the default build (and ci.yml's `--workspace
# --all-targets` archive, which passes NO `--features`) compiles them EMPTY and runs
# ZERO of them — this leg is the ONLY thing that executes them in CI. No new dep
# (`abox = []`, reuses sparq-core + rustc-hash + oxrdf already in the default dep
# set). Verified locally: `cargo test -p sparq-reason-el --features abox` green.
- name: "sparq-reason-el (abox — ABox realisation + whole-ontology consistency)"
  crate: "sparq-reason-el"
  features: "abox"
  test: true

# [FABLE-5] sq-wy3i6 (Phase E4): sparq-reason-el's `par` feature — deterministic parallel
# (bulk-synchronous scoped-worker) CR saturation: `Classifier::classify_par` /
# `classify_graph_par`. Same gap as the legs above: tests/par_differential.rs is
# `#![cfg(feature = "par")]` (par-vs-sequential closure differentials over the CR1–CR6 +
# CR-Self fixture corpus incl. el_smoke.ttl, the delayed-filler CR4 / CRs1-reflexive-link
# mutation-witness fixtures, a multi-chunk synthetic-scale case, and the repeated-run
# determinism stress) and the whole par engine in classify.rs is `#[cfg(feature = "par")]`,
# so the default build compiles them EMPTY and runs ZERO of them. This leg is also the ONLY
# thing that exercises the par-WITHOUT-rbox link-apply arm (a `not(feature = "rbox")` code
# path). No new dep (`par = []`, std scoped threads only). The W3C-corpus differential is
# the ci.yml el-suite lane (`--features el-suite,el-suite-par`).
- name: "sparq-reason-el (par — E4 deterministic parallel saturation)"
  crate: "sparq-reason-el"
  features: "par"
  test: true

# [FABLE-5] sq-wy3i6: the COMBINED par leg — `par` composed with `rbox` (CR10/CR11) and
# `cdomain` (CR7–CR9). Its OWN leg per rule (b) of feature-matrix.yml's guard: `par`+`rbox`
# swaps the parallel apply phase onto the `add_link_rbox` role-closure arm (a
# `not(feature = …)` split from the plain-par leg above), and the
# `cfg(all(feature = "par", feature = "rbox"))` / `…"cdomain"` fixtures in
# tests/par_differential.rs (property chains / transitivity / faceted ranges under the
# parallel engine) compile EMPTY on the plain `par` leg — this leg is the only thing that
# runs them.
- name: "sparq-reason-el (par+rbox+cdomain — parallel saturation, full rule set)"
  crate: "sparq-reason-el"
  features: "par,rbox,cdomain"
  test: true
