# Feature-matrix fragment for `sparq-server`.
#
# One self-contained leg mapping per opt-in feature(group). Assembled into the
# feature-matrix `opt-in-features` strategy by scripts/assemble-feature-matrix.py
# (the `setup` job emits the combined matrix as JSON consumed via fromJSON). Adding a
# leg for THIS crate edits no OTHER crate's fragment, so concurrent opt-in-feature PRs for
# DIFFERENT crates never textually conflict (bead sq-ibrze). [OPUS-4.8]
# SCOPE ([SONNET-4.6] issue #2384): adding/removing/renaming a leg changes the emitted
# leg-name set, and THAT is a TWO-FILE change — this fragment AND
# scripts/tests/feature-matrix-legnames.golden.txt. Every other edit here (features,
# test, tier, tier-reason, comments) leaves the name set alone and is a SINGLE-file
# change. See README.md in this directory.
#
# CONTRACT (gate-critical): each leg's emitted check-run NAME is `opt-in <name>`,
# discovered as a REQUIRED check by the ci-summary `gate` aggregator BY NAME. Keep
# `name` free of the whole words "advisory"/"informational" or the leg stops gating.
# Fields: name (required -> check-run name), crate (required), features (required,
# non-empty comma list), test (bool — run `cargo test` for the leg).

# sparq-server: discoverable-federation descriptors, the SERVICE passthrough,
# time-travel reads, the geof: registry, and the durable-write test seam — all
# opt-in, combined into one compile of the async server.
- name: "sparq-server (federation-descriptors, service, time-travel, geo, test-seams)"
  crate: "sparq-server"
  features: "federation-descriptors,service,time-travel,geo,test-seams"
  test: true

# [GPT-5.6] sq-lsp7k.5.2: the default-OFF grouped facet-count endpoint (`POST /facets`).
# The route, `ServerConfig::facets` field, env arm, handler, `sparq-introspect` dependency, two
# direct config tests, and `tests/facets.rs` end-to-end suite are all cfg-gated, so this dedicated
# leg compiles, clippies, and executes the real double-opt-in/read-auth/snapshot-count surface.
- name: "sparq-server (facets — POST /facets grouped counts)"
  crate: "sparq-server"
  features: "facets"
  test: true

# [GPT-5.6] sq-lsp7k.9.3: the default-OFF prefix-completion endpoint (`GET /complete`). The
# route, config/env flag, AppState generation cache, pure-index sparq-text dependency, direct
# config tests, and `tests/complete.rs` stale-generation end-to-end witness are all cfg-gated.
- name: "sparq-server (complete — GET /complete prefix completion)"
  crate: "sparq-server"
  features: "complete"
  test: true

# [OPUS-4.8] sq-21kp: the OPT-IN per-query access audit log (CDMC CD-2 /
# ISO 27001 A.8.15 / EU CRA logging) — exercises tests/audit_log.rs, which is
# `#![cfg(feature = "audit-log")]` and so never ran under the default build.
# `audit-log` pulls `server` (it operates on the async HTTP Response + tracing);
# composed with the same server feature set as the leg above so it compiles + the
# gated test is discovered and actually executes.
# [OPUS-4.8] sq-vya1: `access-audit` ADDED here. tests/access_audit.rs is
# `#![cfg(feature = "access-audit")]` and the 7 in-src `access_audit::tests::*`
# unit tests are `#[cfg(feature = "access-audit")]`, so the default build (and
# EVERY prior leg) compiled them EMPTY and ran ZERO of them — the silent gap this
# bead closes. `access-audit` only pulls `server` (no conflict — it is a SECOND,
# independent audit sink alongside `audit-log`), so it co-enables cleanly with this
# leg's set; verified: `cargo nextest list -p sparq-server --features access-audit,…`
# now shows the 9 access_audit tests, and the leg's `clippy --all-targets -D
# warnings` gates the feature surface. Folded in here (not a new leg) to avoid a
# near-duplicate full-server-compile.
- name: "sparq-server (audit-log, access-audit + server features)"
  crate: "sparq-server"
  features: "audit-log,access-audit,federation-descriptors,service,time-travel,geo,test-seams"
  test: true

# [OPUS-4.8] sq-dxhb: the Triple Pattern Fragments / Linked Data Fragments READ-ONLY
# source endpoint (`GET /tpf`). The handler + the Hydra paging vocabulary + the
# `tpf::evaluate` pager + the in-module `tpf` unit tests are all `#[cfg(feature =
# "tpf")]`, so the DEFAULT build never compiled, clippy'd or tested this code. This
# leg builds it, runs `clippy --all-targets -D warnings` with the feature ON (the real
# gate), and runs the crate's tests — which now include the gated `tpf` page/Hydra
# suites. `tpf` pulls `server`, so the full async server compiles under it.
- name: "sparq-server (tpf)"
  crate: "sparq-server"
  features: "tpf"
  test: true

# [OPUS-4.8] sq-dxhb: bind-restricted Triple Pattern Fragments (brTPF). `brtpf` implies
# `tpf` and additionally compiles `tpf::evaluate_brtpf`, the `values`/POST bindings
# parser, the `merge_term`/`merge_predicate` semi-join pushdown and the extra
# `hydra:mapping` control — all `#[cfg(feature = "brtpf")]`. The in-module `brtpf` test
# mod (binding restriction, dedup union, empty-bindings==plain-TPF, page partitioning)
# is gated the same way, so it only runs when this feature is on. This leg is what
# actually compiles + clippies + tests the brTPF + Hydra-paging code the PR adds.
- name: "sparq-server (brtpf)"
  crate: "sparq-server"
  features: "brtpf"
  test: true

# [OPUS-4.8] sq-vczh2 (epic sq-2m6zm): the OPT-IN verifiable terse-transpiler endpoint
# (`POST /terse/transpile`). The `terse_transpile_endpoint` handler, the `ServerConfig::terse`
# field + its Default/from_env arms, the route mount and the `tests/terse_transpile.rs`
# integration suite are ALL `#[cfg(feature = "terse")]`, so the DEFAULT build never compiled,
# clippy'd or tested this code. This leg builds it, runs `clippy --all-targets -D warnings`
# with the feature ON (the real gate), and runs the crate's tests — which now include the
# 8-test gated `terse_transpile` suite (opt-in 404, keyword-echo contract, identity
# pass-through, loud-fail 400s, 405, read-auth). `terse` pulls `server`, so the full async
# server compiles under it. Verified: `cargo nextest list -p sparq-server --features terse`
# shows the 8 terse_transpile tests.
- name: "sparq-server (terse)"
  crate: "sparq-server"
  features: "terse"
  test: true

# [GPT-5.6] sq-abhuq: outbound gzip/zstd response negotiation is opt-in and its
# middleware + integration suite are cfg-gated, so compile and test the real
# response-compression surface explicitly.
- name: "sparq-server (response-compression)"
  crate: "sparq-server"
  features: "response-compression"
  test: true

# [GPT-5.6] sq-oprna.6: the default-off h1+h2 auto-builder TCP path, including real TLS/ALPN
# negotiation, HTTP/1.1 fallback, and the inherited slow-loris header deadline integration test.
- name: "sparq-server (http2)"
  crate: "sparq-server"
  features: "http2"
  test: true

# [GPT-5.6] sq-oprna.3: encrypted HTTP/3 over a second UDP listener, including the
# real cross-protocol SPARQL serialization parity integration test.
- name: "sparq-server (http3)"
  crate: "sparq-server"
  features: "http3"
  test: true

# [OPUS-4.8] sq-2999l (gh-906): the OPT-IN CDC change-stream poll endpoint (`GET /streams`) in the
# Amazon-Neptune-Streams `GetRecords` shape, over the merged durable change-stream (sq-b4fns /
# #1223). The `streams_endpoint` handler, the `streams::*` pure builder (iterator-type resolution +
# per-quad flattening + continuation maths) + its 9 in-module unit tests, the
# `ServerConfig::change_stream_dir` field + its Default/from_env arms, the `AppState` change-log
# field + the commit-recording hook on the update path, the route mount, and the
# `tests/streams.rs` integration suite are ALL `#[cfg(feature = "change-stream")]`, so the DEFAULT
# build never compiled, clippy'd or tested this code. This leg builds it, runs `clippy
# --all-targets -D warnings` with the feature ON (the real gate), and runs the crate's tests —
# which now include the 9 `streams::tests` unit tests AND the 9-test gated `tests/streams.rs`
# integration suite that drives the REAL durable round-trip: write commits over the HTTP UPDATE
# path → GET `/streams` from an offset → assert the EXACT ordered change sequence + the
# continuation token (plus the opt-in 404, the four iterator types, `limit` paging/resume, the
# fail-closed 400 on a missing anchor, the read-auth gate, and on-disk durability). `change-stream`
# pulls `server` + `sparq-serve/change-stream` (no new external dependency — it reuses
# sparq-engine's `serialize-rdf` N-Quads diff + the backup family's FNV-1a digest + the already-
# present axum/serde_json), so the full async server compiles under it. Verified locally: `cargo
# test -p sparq-server --features change-stream` runs both suites; `clippy --all-targets -D
# warnings` is clean in the default, change-stream, and all-features states.
- name: "sparq-server (change-stream — GET /streams CDC poll endpoint)"
  crate: "sparq-server"
  features: "change-stream"
  test: true

# [FABLE-5] sq-snopa.6 (issue #992 FR-4): the OPT-IN Solid WAC/ACP HTTP authorization surface
# (`POST /authz/decide`+`/wac-allow`+`/query`), a fail-closed HTTP shell over the `sparq-solid`
# library authoriser. The `crate::solid_authz` module (its handlers + the pure helpers + 12
# in-module unit tests), the `ServerConfig::solid_authz` field + its Default/from_env arms, the
# route mount, the optional `sparq-solid` dependency, and the `tests/solid_authz.rs` integration
# suite are ALL `#[cfg(feature = "solid-authz")]`, so the DEFAULT build never compiled, clippy'd or
# tested this code — and, crucially, the LOAD-BEARING FAIL-CLOSED integration tests would never run
# in CI without this leg (the structural guard `scripts/check-feature-test-execution.py` flags
# exactly that gap). This leg builds it, runs `clippy --all-targets -D warnings` with the feature ON
# (the real gate), and runs the crate's tests — which now include the 12 `solid_authz::tests` unit
# tests AND the 10-test gated `tests/solid_authz.rs` integration suite that drives the REAL axum
# path: the opt-in 404, a real allow with FR-5 `governingAcl`/`aclLink` provenance, and the
# fail-closed denies (anonymous 403, unparseable-dataset 400, unknown-mode 403, ungranted-mode 403,
# grant-less query → zero rows, read-auth 401/200). `solid-authz` pulls `server` + optional
# `sparq-solid` (no new EXTERNAL dependency — sparq-solid is a workspace crate that transitively
# reuses the already-present sparq-engine + oxrdf), so the full async server compiles under it.
# A dedicated single-feature leg (not folded into a broader compile) mirrors the `tpf` / `terse` /
# `change-stream` pattern above and keeps the access-control soundness surface's tests executing on
# their own discoverable check-run. Verified locally: `cargo test -p sparq-server --features
# solid-authz` runs both suites; `clippy --all-targets -D warnings` is clean in the default,
# solid-authz, and all-features states.
- name: "sparq-server (solid-authz — POST /authz/* WAC/ACP HTTP surface)"
  crate: "sparq-server"
  features: "solid-authz"
  test: true

# [FABLE-5] sq-lrtc3.1 + sq-3mu76 (epic sq-lrtc3): `odrl-authz` (default OFF) — the OPT-IN ODRL
# lane on `POST /authz/query` AND (sq-3mu76) the advisory `POST /authz/decide` /
# `POST /authz/wac-allow` endpoints (ODRL as THE access-control language for SPARQL queries,
# fail-closed; the advisory surfaces never report an allow the query lane refuses to honour —
# read-scoped advertisement until the sq-lrtc3.2 action contract lands). NOTE the leg NAME
# below deliberately keeps its original `/authz/query` wording: check-run names are the gate
# aggregator's discovery contract and must stay byte-identical (this file's header).
# The `solid_authz` ODRL branch, the `ServerConfig` wiring, the optional `sparq-policy`
# dependency, and the `tests/odrl_authz.rs` integration suite are ALL `#[cfg(feature =
# "odrl-authz")]`, so the DEFAULT build never compiled, clippy'd or tested this code — the
# structural guard C1 (scripts/check-feature-test-execution.py) flags exactly that gap; this
# leg is its executor. It builds the feature, runs `clippy --all-targets -D warnings` with it
# ON (the real gate), and runs the crate's tests — which now include the gated
# `tests/odrl_authz.rs` suite: the differential decision matrix (HTTP-lane decision ==
# `sparq_policy::evaluate` oracle for permit / prohibit / deny-overrides / constraint /
# conditional(recipient) / fail-closed), the two-requesters-disjoint-rows pillar, the
# fail-closed HTTP refusals (anonymous, non-read mode, targetless rule, unimplementable
# conflict strategy) and the dormant-lane no-ODRL invariant. `odrl-authz` implies
# `solid-authz` + pulls `sparq-solid/odrl-bridge` + optional `sparq-policy` (workspace crates
# only — no new external dependency). A dedicated single-feature leg mirrors the
# `solid-authz` / `tpf` / `terse` pattern above, keeping the access-control soundness
# surface's tests executing on their own discoverable check-run.
- name: "sparq-server (odrl-authz — ODRL lane on POST /authz/query)"
  crate: "sparq-server"
  features: "odrl-authz"
  test: true

# [SONNET-4.6] sq-pfae.17: `solid-authz-trust` (default OFF) — the DOUBLE-OPT-IN stateless
# trust-graph extension to `POST /authz/decide`. The `solid_authz_trust` config field, the
# `trust_authz_decide` handler, and the 11-test gated `tests/solid_authz_trust.rs` integration
# suite are ALL `#[cfg(feature = "solid-authz-trust")]`, so the DEFAULT build never compiled,
# clippy'd or tested this code. This leg builds it, runs `clippy --all-targets -D warnings`
# with the feature ON, and runs the crate's tests — including the 11 gated tests that cover
# the fail-closed invariant, the double-opt-in config flag, the trust-block parse gates, and
# the WAC fallthrough paths. `solid-authz-trust` implies `solid-authz` + pulls `sparq-trust`
# (with `cert-graph`) + `sparq-zk` (for key-parsing), all workspace crates.
# HONEST SCOPE: tests exercise the HTTP plumbing and fail-closed gates; the cryptographic path
# is covered by sparq-trust's own unit tests (the ZK estate is externally UNAUDITED, sq-qhy4).
- name: "sparq-server (solid-authz-trust — stateless trust-graph /authz/decide extension)"
  crate: "sparq-server"
  features: "solid-authz-trust"
  test: true

# [FABLE-5] sq-lsp7k.10: the OPT-IN named-parameterized-template REST surface
# (`/templates` — CRUD + typed fail-closed invocation; UPDATE-template invocations
# write-gated through the same sequenced-writer path as /sparql updates). The
# `crate::templates` module, the routes, the config fields and tests/templates.rs are all
# `#[cfg(feature = "templates")]`, so the default build compiles them EMPTY and runs ZERO
# of the 9 e2e tests (opt-in 404 posture, CRUD round-trip, fail-closed registration +
# invocation, the gated-update posture incl. the hostile-literal injection scenario over
# HTTP, --templates-file persistence). `templates` pulls `server` +
# `sparq-engine/templates` (in-workspace; no new external dep).
- name: "sparq-server (templates — named parameterized template REST surface)"
  crate: "sparq-server"
  features: "templates"
  test: true

# [SONNET-4.6] sq-qsm5z: the OPT-IN running-query registry (`GET /queries` list +
# `DELETE /queries/{id}` cooperative cancel). The `QueryRegistry` struct, `QueryGuard` RAII
# type, `registry_fingerprint` helper, `list_queries_handler` + `cancel_query_handler` handlers,
# the `AppState::running_queries` field, the route mounts, and the in-module
# `query_registry_tests` unit tests are ALL `#[cfg(feature = "query-registry")]`, so the
# DEFAULT build never compiled, clippy'd or tested this code. This leg builds it, runs
# `clippy --all-targets -D warnings` with the feature ON (the real gate), and runs the
# crate's unit tests — which now include 10 gated `query_registry_tests` (fingerprint
# stability + distinctness, RAII guard cleanup on normal return, RAII guard cleanup on unwind,
# cancel flips the flag to true, cancel unknown id returns false, multiple entries listed and
# individually cancellable; plus sq-t1isr EXPLAIN-path tests: entry present while guard alive,
# cancel flag flippable, guard cleans up on panic). `query-registry` pulls only `server` (no
# new dependency). Zero cost when feature is OFF: no `AppState` fields, no routes, byte-identical
# to before. sq-t1isr ([SONNET-4.6]) wires the SAME registration into the EXPLAIN ANALYZE path.
- name: "sparq-server (query-registry — GET /queries list + DELETE /queries/{id} cancel)"
  crate: "sparq-server"
  features: "query-registry"
  test: true

# [SONNET-4.6] sq-3bz2w (follow-up to sq-u4lgr/#902): the OPT-IN slow-query log — the server
# policy layer over sparq-engine's `explain_json::SlowQueryRing`. The `slow_query` module (the
# `SlowQueryLog` policy type + the `slow_queries_endpoint` handler), the two `ServerConfig`
# fields, the `AppState::slow_queries` field + accessor, the `/admin/slow-queries` route mount,
# the recording seam in `run_query`, the two `main.rs` flags, the in-module unit tests and
# `tests/slow_query_log.rs` are ALL `#[cfg(feature = "slow-query-log")]`, so the DEFAULT build
# never compiles, clippies or tests any of it. This leg builds it, runs `clippy --all-targets
# -D warnings` with the feature ON (the real gate), and runs both suites — 8 unit tests
# (threshold below/at/zero, unplannable query skipped, ring bound keeps the slowest, the
# envelope's honesty constants, the handed-in measurement, capacity clamp) and 5 HTTP
# integration tests (disarmed => 404, WRITE-gated even when reads are open, a real /sparql
# query recorded with a real measurement, an unreachable threshold records nothing, the
# envelope declares its boundaries). `slow-query-log` pulls `server` + `sparq-engine/explain-json`
# (in-workspace, itself dependency-free); no new external dependency.
- name: "sparq-server (slow-query-log — GET /admin/slow-queries worst-N view)"
  crate: "sparq-server"
  features: "slow-query-log"
  test: true

# [FABLE-5] sq-37f1a (issue #2718): the COMBINED `odrl-authz,solid-authz-trust` state — the
# feature INTERACTION the two single-feature legs above never exercise. Cargo feature
# unification makes `odrl-authz` turn on `sparq-solid/odrl-bridge`, which changes the
# `PodStore` post-materialize path (`reconcile_bridged_after_static` → `BridgeLedger::refresh`)
# that EVERY trust-path decide then runs through — exactly where the sq-37f1a regression hid
# (an empty static WAC closure lost its `<urn:sparq:auth>` "materialized" marker, flipping a
# definitive 403 deny into a retryable 503; each single leg stayed green). This leg compiles
# both features together, runs `clippy --all-targets -D warnings` with them ON, and runs the
# crate's tests — the full gated `tests/solid_authz_trust.rs` suite against the odrl-bridge
# store path (including the injection-gate 403 pin) plus the both-features-only guard test
# `decide_refuses_trust_block_combined_with_odrl` (sq-3mu76), which NO single-feature leg can
# ever compile. Workspace crates only — no new external dependency.
- name: "sparq-server (odrl-authz + solid-authz-trust combined)"
  crate: "sparq-server"
  features: "odrl-authz,solid-authz-trust"
  test: true
