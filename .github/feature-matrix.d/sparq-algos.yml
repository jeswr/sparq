# Feature-matrix fragment for `sparq-algos`.
#
# [GPT-5.6] sq-lsp7k.15: exact all-pairs centrality is default-OFF, and
# tests/centrality_extended.rs is feature-gated. The workspace lane covers the
# default build; this leg compiles, lints, and runs the feature-ON suite.
- name: "sparq-algos (centrality-extended — exact centrality algorithms)"
  crate: "sparq-algos"
  features: "centrality-extended"
  test: true
