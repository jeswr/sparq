# [GPT-5.6] The bounded typo-tolerant lookup and its integration test are
# default-OFF behind `fuzzy`, so execute them explicitly.
- name: "sparq-text (fuzzy)"
  crate: "sparq-text"
  features: "fuzzy"
  test: true
