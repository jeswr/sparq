# [OPUS-4.8] ast-grep rule: every `impl Default for $T` (any path qualification).
# List-stratum A/B task. See ../../../../.claude/skills/ast-grep/SKILL.md §2.3.
id: impl-default
language: rust
rule:
  kind: impl_item
  has:
    field: trait
    regex: '(^|::)Default$'
