# Narrow, explicitly-justified allowlist

A changelog entry must be able to RECORD the rename. The inline marker is the only
line-level escape, and it carries a mandatory justification reviewed in the diff:

- Renamed `TrustEnvelope` to `ContractRequest`. terminology-allow: CHANGELOG record of the sq-6syab.4 rename itself
