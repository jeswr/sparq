# Fixture research note (paper-evidence verifier self-test)

This is a fixture-only doc, NOT a real project record. It exists so the doc-anchor tier of the
verifier has a committed source to resolve an exact quote against.

The measured heuristic-grading arm reached its stated accuracy on the fixture task set, and the
verbatim quote below is what a doc-anchor binding pins:

The demo arm passed the fixture oracle set exactly as designed.
