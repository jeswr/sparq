# Distinct dataset helpers

Implemented behind the default-off `proposed-distinct` feature as the inherent
`Dataset::subjects_of` and `Dataset::objects_of` methods in the crate root.
Unlike the other implemented proposals, this feature has no `proposed::distinct`
module.

<!-- [SONNET-4.6] sq-1rg2q.1 -->
