# [OPUS-4.8] sq-ixc3.8 — build artifacts for the operational GUI frontend.
/.next/
/out/
/node_modules/
next-env.d.ts
*.tsbuildinfo

# The wasm bundle is synced in from js/wasm at build time (a build artifact, not source).
/public/wasm/

# [OPUS-5] sq-ixc3.17 — the ZK tool's circuit ACIR is synced in from site/public/zk at build
# time (the one committed copy lives there), so it is a build artifact here too.
/public/zk/
