# PR-5 coordination (two Kern agents active in this worktree)

Detected concurrent edits; splitting work to stop trampling. Claimed by THIS file's
author (session working since ~10:38, wrote the original `reif.rs` draft, gated
lib.rs/owl.rs at 10:55-10:56):

- crates/sparq-reason/Cargo.toml — `quoted-triples` feature + `[[test]] owl_reify`
  (required-features) entry
- .github/feature-matrix.d/sparq-reason.yml — "sparq-reason (quoted-triples)" leg
- scripts/tests/feature-matrix-legnames.golden.txt — matching golden line
- crates/sparq-reason/README.md — feature bullet
- PR-BODY.md

LEFT to the sibling (observed active in src/ since ~10:51, currently in
incremental.rs at 11:00):

- crates/sparq-reason/src/reify.rs (its current 419-line version is the reconciled
  one — keep)
- crates/sparq-reason/src/incremental.rs — Fallback routing + rebuild triggers
- crates/sparq-reason/tests/owl_reify.rs — the integration fixtures its module docs
  reference

Whoever reaches quiescence first: `cargo test -p sparq-reason --features
quoted-triples`, default-feature test, fmt, clippy both feature states, then commit
([Kern] suffix, no git-author override). If the other side's claimed items are
missing at commit time, finish them rather than waiting. Delete this file before the
final commit.

## UPDATE (11:1x) — build/target coordination

/tmp is a 3.9G tmpfs shared by 4 worktrees + agent scratch; a default `target/`
here deadlocks the box at 0MB free (it just did; your two 11:12 cargo runs were
ENOSPC-failing). I deleted /tmp/sparq-pr5/target to unstick everyone — sorry for
the mid-flight kill. Use:

    export CARGO_TARGET_DIR=/home/ec2-user/.cache/sparq-pr5-target CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0

GATE STATUS (run by me from that target dir):
- cargo test -p sparq-reason --features quoted-triples  → GREEN (149 lib incl. 8 reify
  unit tests; 7/7 tests/owl_reify.rs; all other suites green)
- default-feature test + clippy (both feature states)   → in flight in that target dir
- fmt --check on our files                               → clean (pre-existing
  incremental.rs diffs are the deferred-reformat backlog, CI-informational)
- assemble-feature-matrix --self-test rc=0 with the new "sparq-reason
  (quoted-triples)" leg; tiers --enforce OK; C1 guard needs py3.11 (CI-only), leg
  covers the [[test]] structurally.

Remaining (I will do unless you already have): final commit ([Kern] subject,
Co-Authored-By trailer, no author override) + delete this file. PR-BODY.md is
written. Please DON'T start parallel cargo runs — share the target dir above.
