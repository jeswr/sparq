{
  "pr": 1927,
  "date": "2026-07-11",
  "reason": "[SONNET-4.6] sq-6aefu: the order_bindings worker-reinstall fix edits crates/sparq-engine/src/exec.rs, adding snapshot()+worker_install() calls at BOTH PAR_THRESHOLD rayon key-build sites (top-k index-carry and full stable-sort paths) and a new #[cfg(all(test, feature = \"parallel\"))] test module. The snapshot/worker_install code is inside #[cfg(feature = \"parallel\")] blocks (rayon is absent from the sparq-wasm default feature set), so ZERO new executable code enters the feature-OFF wasm bundle. The test module is gated #[cfg(all(test, feature = \"parallel\"))] — also excluded. The feature-OFF sparq-wasm bundle changes by +0 bytes (+0.000%). VERIFIED by wasm-tools objdump: CODE section is byte-for-byte identical (same 0x2607..0x16ac81 offset, same 1476219 bytes, same 2495 function count); DATA section is the same size (69460 bytes) with the same string set (531 strings, 0 added / 0 removed); 31 bytes differ across the DATA section, all representing a uniform +19-byte shift of &str fat-pointer offsets (a constant emission-order offset from adding comments and test-module tokens into the source file) — one pointer encodes the shift in multi-byte little-endian (+19 as 0x00002df0→0x00002e03). No order_bindings, worker_install, worker_reinstall, or sq-6aefu symbol appears in either bundle. Query/sort behaviour is byte-identical; no bench/perf-baseline.json floor change."
}
