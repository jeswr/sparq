{
  "pr": 1901,
  "date": "2026-07-10",
  "reason": "[OPUS-4.8] sq-98w7z.1: fixing an O(n²) hang in the ALWAYS-COMPILED parallel Turtle terminator scanner (crates/sparq-core/src/lib.rs next_terminator: bound the second memchr3 to the window before the first `. < #` so a quote/backslash-free body no longer re-walks the whole tail per statement) edits code that compiles into the feature-OFF sparq-wasm bundle, so its bytes move by design. MEASURED (work-box reproduction of vectorized-feature-off leg2: base tree 6ec1a2da3 vs head, both `cargo build --profile release-wasm -p sparq-wasm --target wasm32-unknown-unknown` with default features, NO --features vectorized): SIZE delta = +0 bytes (1580701 -> 1580701, +0.0000%, well inside the ±2% wasm_bundle_bytes band). VERIFIED not a feature leak with `wasm-tools objdump`: the CODE section is BYTE-FOR-BYTE IDENTICAL to base (same 0x2607..0x16ac81 span, same 1476218 bytes, same 2495 function count) so executable behaviour is unchanged and no parser/memoize code leaked; the DATA section is the SAME size (69459 bytes, same 2-count) with an IDENTICAL string set (0 strings added / 0 removed). Only 279 bytes differ across the whole 1.58 MB bundle, ALL inside the data section and ALL a shift of `&str` fat-pointer addresses — a constant emission-order offset from the tighter scan-loop codegen, i.e. identical content emitted at slightly shifted locations. No `next_terminator`/`regex_cache`/`query_now`/`rand_unit`/`turtle_chunks` symbol appears in either bundle (release-wasm strips symbols). The NOW()/RAND()/REGEX-memo builtin work in this PR is native-only (#[cfg(not(target_arch = \"wasm32\"))] / behind the native regex+parallel features) so it contributes nothing to the wasm bundle. Query/parse behaviour with the feature OFF is byte-identical; no bench/perf-baseline.json floor change accompanies this declaration (size delta 0 bytes)."
}
