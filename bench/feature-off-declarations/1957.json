{
  "pr": 1957,
  "date": "2026-07-11",
  "reason": "[FABLE-5] sq-qeltv: INTENTIONAL always-compiled behaviour change — a SPARQL 1.1 conformance bug fix in crates/sparq-engine/src/exec.rs `eval_function_inner` (the single dispatch site shared by interpreted + compiled expression paths, compiled into the feature-OFF sparq-wasm bundle): isIRI/isBlank/isLiteral/isNumeric on an UNBOUND or errored operand now raise a §17.2 type error (was: lenient `false`), and STR on a blank node is a §17.4.2.5 type error (was: leaked the engine-internal bnode label). This is deliberately NOT feature-gated (it is a conformance fix, not an opt-in capability), so the feature-OFF wasm bytes move BY DESIGN — this is not the cfg-gated line!()/inlining drift pattern and not a vectorized/default-path leak (the diff contains zero cfg attributes; leg 3 cfg-audit green). MEASURED (work-box reproduction of leg2: base tree 241b6726e vs head merged with that main tip, both `cargo build --profile release-wasm -p sparq-wasm --target wasm32-unknown-unknown`, default features): size delta +1672 bytes (1580701 -> 1582373, +0.106%, well inside the ±2% wasm_bundle_bytes band — the bench.yml ratchet leg is green on the PR). VERIFIED with `wasm-tools objdump`: the ENTIRE size delta is the code section (1476218 -> 1477890 bytes, same 2495 function count = the new type-error match arms); the data section is the SAME size (69459 bytes, 2 count) with an IDENTICAL string set (0 semantic strings added/removed; only constant-pool packing/fat-pointer addresses shifted). The behavioural delta is exactly the two fixed §17.2 corners, direct-tested in tests/eval_semantics.rs::type_error_builtins and pinned by the proptest_eval regression seed; W3C conformance scoreboard unchanged (1225 pass / 0 fail)."
}
