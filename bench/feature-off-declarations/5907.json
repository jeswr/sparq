{
  "pr": 5907,
  "bead": "sq-0lk",
  "date": "2026-09-02",
  "reason": "[OPUS-5] sq-0lk is the one-time workspace-wide `cargo fmt --all` reformat plus the flip of `cargo fmt --all --check` from `continue-on-error: true` to a hard gate in ci.yml's `clippy + fmt (gate)` job. It moves the feature-OFF sparq-wasm bundle bytes for a purely POSITIONAL reason and declares that intent here. WHAT CHANGED: 773 .rs files and no manifest at all — `Cargo.toml`/`Cargo.lock` are untouched by this PR, so the resolved default dependency graph and feature set of sparq-wasm are bit-for-bit the same as the base tree's. VERIFIED IN THE AUTHORING CHECKOUT with the pinned formatter (rustfmt 1.9.0-stable, toolchain 1.97.1 from rust-toolchain.toml): for 771 of the 773 files, the head content is BYTE-IDENTICAL to a single rustfmt pass over that file's own base-tree content, i.e. the diff is exactly what the tool emits and contains no hand-written line. `crates/sparq-cli/src/tabular.rs` is byte-identical after the SECOND pass — rustfmt is not single-pass convergent on one `return Err(format!(...))` match arm there, the non-convergence documented in rustfmt.toml; the committed tree is the fixed point, which is why the single `--check` pass CI runs is clean. The 773rd file, `crates/sparq-acbench/src/financial.rs`, carries the ONE hand edit in this PR: the reformat expanded `build_expected_decisions` from under to over the 100-line `clippy::too_many_lines` threshold (106/100) that the crate opts into via `#![warn(clippy::pedantic)]`, so its five verbatim-duplicated per-request blocks were factored into one `push_read_decisions` helper — behaviour-preserving (the same requests are pushed in the same order; the crate's 141 tests pass) and it shortens the function instead of silencing the lint. sparq-acbench is a `publish = false` benchmark crate and is NOT in the sparq-wasm dependency graph, so it contributes zero bytes to this bundle either way. WHY THE BYTES MOVE: the workspace release profile sets `panic = \"abort\"` but not `panic_immediate_abort`, so every panicking call site still emits a `core::panic::Location` record that embeds its own LINE NUMBER as a constant. Reformatting rewraps always-compiled code in crates the bundle does link — sparq-core (20 files), sparq-engine (72), sparq-substrate (7) and sparq-wasm (10) — so every such line constant below a rewrapped expression moves with it. This is the same mechanism declared in #4691, #4443, #4369 and #4012, applied workspace-wide rather than to one insertion point. This is NOT a default-path or `vectorized` leak, the invariant leg 2 exists to protect: no cargo feature becomes default-on, no dependency enters the default wasm graph, no `unsafe` is introduced, no `vectorized`/SIMD code path becomes reachable, no gate is widened or removed, and no default-path behaviour changes — rustfmt rewrites whitespace and line breaks only. NO byte delta is claimed here: reproducing leg 2 needs a second full release-wasm build of the base tree, which the authoring checkout has no room to stage, so the base-vs-head comparison was NOT run locally — CI leg 2 performs the actual byte comparison. The SIZE of the accepted change stays governed SEPARATELY, unchanged, by the `metrics.wasm_bundle_bytes` +/-2% floor ratchet in bench/perf-baseline.json / bench.yml; a pure line-number shift is not expected to approach that band."
}
