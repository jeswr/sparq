{
  "pr": 2401,
  "bead": "sq-q4apb",
  "date": "2026-07-17",
  "reason": "[OPUS-4.8] sq-q4apb (#2396): feature-OFF posture declaration for the new hosted-web `Store.deriveForm` forms bridge. The whole capability is behind the default-OFF `forms` feature: `crates/sparq-wasm/src/lib.rs` gates the module with `#[cfg(feature = \"forms\")] mod forms;`, and both new deps (`sparq-forms`, `serde_json`) are `optional = true` and pulled only by `forms = [\"dep:sparq-forms\", \"dep:serde_json\"]`. The lean default bundle therefore compiles zero forms/serde code. VERIFIED (work-box reproduction of leg2: base tree ec1ededd7 vs branch head 1c3861ae6, both `cargo build --profile release-wasm -p sparq-wasm --target wasm32-unknown-unknown`, default features): the feature-OFF bundle is byte-length IDENTICAL (1554299 -> 1554299, delta +0, +0.000%) but differs at one code-section offset (byte 1471047). `wasm-tools objdump` confirms BOTH trees have an identical code section (1449790 bytes, 2501 function count) and identical data section (69479 bytes, 2 count) — no function added, no string/data added. This is the cfg-gated `line!()`/`std::panic::Location`/inlining drift pattern from inserting the compiled-out `mod forms;` line + comment block into the big always-compiled `lib.rs`, NOT a vectorized/default-path leak (leg3 cfg-audit green; a real leak would have raised the code-section size + function count). The change is intentional and stays inside the wasm_bundle_bytes ±2% ratchet band (it moves 0 bytes)."
}
