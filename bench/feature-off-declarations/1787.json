{
  "pr": 1787,
  "date": "2026-07-08",
  "reason": "sq-7d3dj.30.14: the cluster-materialize opt-in code (cluster::detect hook + eval_bgp_cluster) is fully #[cfg(feature = \"cluster-materialize\")]-gated and NEVER runs on the feature-OFF path — the query SEMANTICS are byte-identical (verified: git diff vs base shows every added Rust item is cfg-gated; no always-compiled code path changed). The feature-OFF sparq-wasm bundle nonetheless differs by 0 bytes (base=head byte length identical, size delta +0, +0.000%) because inserting the ~60 cfg-gated lines into the always-compiled eval_bgp_binary shifts the panic-location line numbers (file:line:col strings from .unwrap()/slice-index #[track_caller] sites) embedded below the insertion point in the release-wasm build (panic=abort keeps those &str literals; strip=symbols does not remove them). Reproduced locally: my exec.rs alone (base lib.rs, no cluster.rs) reproduces the diff; base source + my Cargo.toml matches base exactly, so the empty [features] entry is NOT the cause. Semantics-preserving, NOT accidental vectorization (feature-OFF invariant vectorized-ON==OFF preserved); size unchanged so no perf-baseline floor bump."
}
