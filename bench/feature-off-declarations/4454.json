{
  "pr": 4454,
  "bead": "sq-dzfzq",
  "date": "2026-07-27",
  "reason": "[SONNET-4.6] sq-dzfzq rewrites the index-build body in `crates/sparq-core/src/store.rs`, INCLUDING the `#[cfg(not(feature = \"parallel\"))]` no-threads half — which is exactly the body the feature-OFF `sparq-wasm` bundle compiles (`crates/sparq-wasm/Cargo.toml` takes `sparq-core` with `default-features = false`, so `parallel` is OFF and the wasm build links the no-threads `TripleStore::build_raw_perms`). The build no longer sorts every permutation from scratch: SPO is produced by the existing full 12-digit `radix_sort_rows` plus the ONE dedup, and every other BUILT permutation is DERIVED from an already-sorted run by a column re-map plus a stable sort on its NEW LEADING COLUMN alone — the new always-compiled `TripleStore::derive_perm` and `radix_sort_rows_by_col0` functions, driven by the new `DERIVE_PLAN` const. Replacing the per-permutation full-sort closure with those two functions plus a const table changes the always-compiled sparq-core code the feature-OFF bundle links, so the feature-OFF bytes move INTENTIONALLY. This is NOT an accidental default-path/`vectorized` leak — the invariant this gate exists to protect: NO cargo feature is added or enabled, NO dependency is added (pure `std`), NO `unsafe` is introduced, no existing gate is widened or removed, no `vectorized`/SIMD code path becomes reachable, and nothing outside ingest-time index construction inside `sparq-core` is touched. The feature-OFF INVARIANT (bundle identical with `vectorized` ON vs OFF) is preserved. Result-equivalence is unchanged and is pinned by the pre-existing `from_triples_perms_match_reference_sort` differential test plus the new `derived_perms_match_radix_all_build` (every BUILT permutation must stay BYTE-IDENTICAL to an independent full-radix sort+dedup of the same input, over corpora spanning constant / sub-byte / multi-byte leading columns, heavy duplicates and both id-space boundaries) and `derive_plan_pairs_are_derivable` (the derivability + scheduling invariants of the plan as shipped). NO byte delta is claimed here: the `wasm32-unknown-unknown` std component cannot be installed in the authoring checkout (read-only rustup directory, so the pinned toolchain cannot be installed either), so the base-vs-head comparison was NOT reproduced locally — CI leg 2 performs the actual byte comparison. The accepted SIZE of the change remains governed SEPARATELY, unchanged, by the `metrics.wasm_bundle_bytes` +/-2% floor ratchet in `bench/perf-baseline.json` / `bench.yml`."
}
