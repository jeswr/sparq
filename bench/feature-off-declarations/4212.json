{
  "pr": 4212,
  "bead": "sq-7d3dj.21",
  "date": "2026-07-31",
  "reason": "[OPUS-5] sq-7d3dj.21 intentionally changes ALWAYS-COMPILED sparq-core code on the serial N-Triples path that the feature-OFF sparq-wasm bundle actually links: `nt::parse_chunk` (and, under the `parallel` feature, `nt::parse_quads_chunk`) now establishes the module's CHUNK-UTF8 invariant with one contiguous `validate_chunk_utf8` pass over the chunk buffer, and `nt::str_of` correspondingly drops its per-span `std::str::from_utf8` check plus the `Result`/error-string plumbing at all four span call sites (`decode`, `blank`, `graph_key`, the `@lang` tag), becoming an O(1) `from_utf8_unchecked` conversion guarded by a `debug_assert!`. sparq-wasm builds sparq-core with `default-features = false`, so `parallel` is OFF and the serial `nt::parse_chunk` call site in `Graph::parse_format` (crates/sparq-core/src/lib.rs) is live code in the feature-OFF bundle — moving that error-handling shape is expected to move the bundle bytes. This is NOT a `vectorized` (or any other default-OFF feature) leak: no `#[cfg(feature = ...)]` line is added, removed, or retargeted by this PR, and no default-OFF capability becomes default-ON. Conformance is unchanged-or-stricter, not weakened: N-Triples/N-Quads documents are defined to be UTF-8, and invalid UTF-8 is still rejected with a byte offset — now at the entry point before any span is taken (pinned by `parse_chunk_rejects_invalid_utf8` / `parse_quads_chunk_rejects_invalid_utf8`), with the boundary argument for the unchecked conversion pinned by `multibyte_spans_at_every_delimiter_round_trip`, `escaped_and_multibyte_literal_span_round_trips` and `language_tag_terminated_by_multibyte_errors_cleanly`. The companion sq-7d3dj.21b edits in crates/sparq-core/src/dict.rs are comments and `#[cfg(test)]` tests only, which do not reach the bundle. NO byte delta is claimed here: the wasm32-unknown-unknown std component cannot be installed in the authoring checkout (read-only rustup toolchain), so the base-vs-head comparison was NOT reproduced locally — CI leg 2 performs the actual byte comparison, and the accepted SIZE remains governed separately by the `metrics.wasm_bundle_bytes` floor ratchet (+/-2% band) in bench/perf-baseline.json / bench.yml, which this declaration does not touch."
}
