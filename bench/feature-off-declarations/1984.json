{
  "pr": 1984,
  "bead": "sq-tonhr.2",
  "date": "2026-07-11",
  "reason": "[FABLE-5] sq-tonhr.2: INTENTIONAL always-compiled API addition — crates/sparq-core/src/lib.rs gains `Graph::load_dataset_with_base` (the DATASET companion to the existing `load_str_with_base`: resolves a TriG/JSON-LD document's relative IRIs against a caller-supplied base while preserving named graphs) plus the shared `load_dataset_serial_base` body it and `load_dataset_serial` now both route through. sparq-core is compiled into the feature-OFF sparq-wasm bundle, so the feature-OFF bytes move BY DESIGN. This is deliberately NOT feature-gated: it is a small general-purpose loader entry point mirroring the long-standing always-compiled `load_str_with_base`, needed by the W3C TriG/N-Quads conformance ratchets + parser differential harness this PR adds (base-relative dataset documents addressed by suite-canonical base IRIs); gating a core loader API behind an opt-in feature just to keep bundle bytes still would fragment the loader surface. Not the cfg-gated line!()/inlining drift pattern and not a vectorized/default-path leak — the only cfg attribute in the diff is the pre-existing `jsonld` gate, and the new code is reachable, direct-tested always-compiled API (lib.rs test load_dataset_with_base_resolves_and_preserves_named_graphs). MEASURED by the leg2 CI run itself (run 29171786752, base 517f8e54a tree vs head ce0b6029c tree, same-run same-toolchain byte comparison): 1581613 -> 1582415 bytes, delta +802 (+0.051%), far inside the ±2% wasm_bundle_bytes band (the bench.yml ratchet leg is green on this PR)."
}
