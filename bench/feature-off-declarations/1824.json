{
  "pr": 1824,
  "date": "2026-07-10",
  "reason": "sq-7d3dj.32.2.7: promotes SPQCPRM2 to a versioned on-disk format. The wasm feature-OFF bundle (sparq-core default-features=false: no mmap, no spqcprm2) compiles NONE of the new format machinery — the V2 reader/emitter, EmitFormat, encode_emit, file_magic, Format enum, decode_block_v2_at, zigzag helpers, and FILE_MAGIC_V2 are all `#[cfg(feature = \"mmap\")]` or `#[cfg(feature = \"spqcprm2\")]`, and wasm has neither. Verified in-worktree by building the feature-OFF sparq-wasm bundle from BOTH the merge-base (e728d0f1b) and this head in the same environment: the CODE section (wasm section id=10) is byte-for-byte IDENTICAL between base and head (1472736 bytes both), and the total bundle size is unchanged (1577165 bytes). The only delta is 9 bytes in the DATA section (id=11, same total length 69411), all near offset 1514261 — LLVM reordered #[track_caller] panic-location `file:line:col` `&str` fat-pointers whose line numbers shifted when cfg-gated source lines moved/were inserted in crates/sparq-core/src/compress.rs. Behaviour-identical machine code, same total bundle size (no ratchet floor bump needed), no new dependencies on the default bundle, no unsafe. Same class as PR #1800's declaration — a same-size data-section string-reorder is the sanctioned declaration case, not a feature leak."
}
