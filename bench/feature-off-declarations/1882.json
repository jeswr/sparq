{
  "pr": 1882,
  "date": "2026-07-10",
  "reason": "[OPUS-4.8] sq-jocpn: the opt-in native byte-level Turtle parser adds crates/sparq-core/src/ttl.rs behind #[cfg(feature = \"native-ttl\")] and inserts the gated `mod ttl;` line + several #[cfg(feature = \"native-ttl\")] call-site arms into the always-compiled crates/sparq-core/src/lib.rs (feature declared as `native-ttl = [\"dep:oxiri\"]`, OFF by default). With native-ttl OFF no ttl-parser code is compiled, but inserting the compiled-out cfg tokens + module line into the large always-compiled lib.rs nudges LLVM's static read-only-data emission ORDER, moving the feature-OFF sparq-wasm bundle by +0 bytes (+0.000%, well within the ±2% band). VERIFIED not a feature leak by parsing both bundles with `wasm-tools objdump`: the CODE section is byte-for-byte IDENTICAL to base (same 0x2607..0x16ac81 offset, same 1476218 bytes, same 2495 function count) so executable behaviour is unchanged and zero ttl-parser code leaked; the DATA section is the SAME size (69459 bytes) with the SAME string set (11496 strings, 0 added / 0 removed); no `native-ttl`/`native_ttl`/`ttl::parse`/`src/ttl.rs` symbol appears in either bundle; `oxiri` appears exactly once in BOTH (the pre-existing oxttl→oxiri transitive dep, not the feature's optional dep:oxiri). Only 9 bytes differ across the whole 1.58 MB bundle, all inside the data section and all a uniform +26 (0x1A) shift of `&str` fat-pointer addresses — a constant emission-order offset, i.e. identical content emitted at a slightly shifted location. Query/parse behaviour with the feature OFF is byte-identical; no bench/perf-baseline.json floor change accompanies this declaration (size delta 0 bytes)."
}
