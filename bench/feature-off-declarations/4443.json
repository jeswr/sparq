{
  "pr": 4443,
  "bead": "sq-v8ixk",
  "date": "2026-07-27",
  "reason": "[OPUS-5] issue #3209 / sq-v8ixk adds the `block-bloom` MEASUREMENT harness to crates/sparq-core/src/compress.rs. NO always-compiled CODE is added or changed: every new item is either inside `#[cfg(test)] mod tests` (the harness itself, its column generator, its parametric filter model and its self-check) or inside `#[cfg(feature = \"block-bloom\")] mod block_bloom`, whose feature is OFF by default and is enabled by nothing, so the feature-OFF sparq-wasm build compiles zero bytes of it. The only edits to the shipped filter module are doc-comment text and three private-to-`pub(super)` visibility changes on constants (BITS, HASHES, MIN_AVG_DISTINCT_PER_BLOCK) whose only new reader is the `#[cfg(test)]` harness — a visibility widening with no codegen effect and no new non-test reader, and no gate is widened, removed or added, no dependency is added, no `unsafe` is introduced, and no default-path behaviour changes. The bundle bytes move for a purely POSITIONAL reason, the same mechanism declared in #4369 and #4012: the 34 lines of rationale/scope documentation inserted at lines 121-292 sit ABOVE `pub struct CompressedPerm` (line 266 at base, line 300 at head) and its always-compiled `impl` block, which sparq-wasm links via sparq-core's in-RAM compressed store path (`from_triples_compressed`, `encode`, `decode_block_at`, `range` - cf. the #4032 declaration). Every panic / `#[track_caller]` `Location` line-number constant emitted for that always-compiled code therefore shifts by exactly 34. `Location::line` is a `u32` constant rather than a formatted string, so this is expected to be a same-LENGTH content change, which the byte-for-byte gate detects regardless of size; the accepted SIZE remains governed separately by the `metrics.wasm_bundle_bytes` +/-2% floor ratchet in bench/perf-baseline.json / bench.yml, which a line-number shift is not expected to approach. NO byte delta is claimed here: the wasm32-unknown-unknown std component cannot be installed in the authoring checkout (read-only rustup directory, so the pinned 1.97.1 toolchain cannot be materialised either), so the base-vs-head comparison was NOT reproduced locally - CI leg 2 performs the actual byte comparison."
}
