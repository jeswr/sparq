{
  "pr": 4369,
  "bead": "sq-96hp1",
  "date": "2026-07-27",
  "reason": "[SONNET-4.6] sq-96hp1 adds the OPT-IN `elias-fano` feature (Elias-Fano / Partitioned-Elias-Fano compressed-seek column codecs, `crates/sparq-core/src/eliasfano.rs`). NO always-compiled CODE is added: the whole 890-line module sits behind `#[cfg(feature = \"elias-fano\")]`, the feature is OFF by default and is enabled by nothing, so the feature-OFF sparq-wasm build compiles zero bytes of it and no default-path behaviour changes. This is therefore NOT a leak of a default-OFF feature into the feature-OFF build: no existing gate is widened or removed, no dependency is added (pure `std`), no `unsafe`, and no `PermData`/join call site is rewired. The bundle bytes move for a purely POSITIONAL reason: the 7 lines that declare the gated module (the `#[cfg(feature = \"elias-fano\")] pub mod eliasfano;` pair plus its 5-line rationale comment) are inserted at line 10 of `crates/sparq-core/src/lib.rs`, which is 11,257 lines long and carries the bulk of always-compiled `impl Graph` inline below them. Every panic/`#[track_caller]` `Location` line-number constant emitted for that always-compiled code — which the feature-OFF wasm bundle links via sparq-core — therefore shifts by 7, exactly the panic-location movement declared in #4012. `Location::line` is a `u32` constant rather than a formatted string, so this is expected to be a same-LENGTH content change (the gate compares byte-for-byte, so it is detected regardless of size); the accepted SIZE remains governed separately by the `metrics.wasm_bundle_bytes` +/-2% floor ratchet in bench/perf-baseline.json / bench.yml, which a line-number shift is not expected to approach. NO byte delta is claimed here: the wasm32-unknown-unknown std component cannot be installed in the authoring checkout (read-only rustup directory, so the pinned 1.97.1 toolchain cannot be installed either), so the base-vs-head comparison was NOT reproduced locally — CI leg 2 performs the actual byte comparison."
}
