# Session handover — 2026-06-12 (evening)

Written at session wind-down (90% usage). Previous session transcript (full agent briefs, all
research sub-survey texts): `~/.claude/projects/-Users-jesght-Documents-GitHub-rdfjs-N3-js/b4658078-df2a-48e2-816e-72b7886a55f9.jsonl`
Canonical project state also lives in Claude memory: `sparq-engine-project.md` (same dir, memory/).

## 1. Main repo state

main is PUSHED through `ad9dab1` (full-text merge). ONE LOCAL UNPUSHED merge commit on top:
**memory-tiering** (predstats id-width fix + memtier verdicts). Its gate was RUNNING at handover
(`cargo test --workspace --exclude sparq-py --release` totals + `cargo test -p sparq-core --features mmap`
+ wasm build). **First action next session: re-run that gate (use `grep -a` — BSD grep flips to
binary mode on one test's output), expect ~650+/0 + mmap 40/0 + wasm 1,572,950 ±8B, then
`git push jeswr main`.** If anything fails, the merge commit is the last commit on main — inspect before pushing.

Gate command (house standard):
```
cargo test --workspace --exclude sparq-py --release 2>&1 | grep -aE "^test result" | awk '{s+=$4; f+=$6} END {print "passed:", s, "failed:", f}'
cargo build -p sparq-wasm --target wasm32-unknown-unknown --release && stat -f%z target/wasm32-unknown-unknown/release/sparq_wasm.wasm
```
wasm baseline: **1,572,950** (main checkout). After path-pushdown merges it becomes **~1,590,500**
(+17,550B accepted deliberately: bound-endpoint path traversal compiles into wasm and is wanted there;
decision already reported to Jesse, no objection).

## 2. Worktrees / branches — state and next action

| worktree | branch | state | next action |
|---|---|---|---|
| sparq-parse | custom-parsers | **D4 COMPLETE, clean** — 4 commits to `e2071ec`: CompressedSink (multi-member gzip / multi-frame zstd parallel result compression), SingleMemberGzipSink (browser-safe pigz-style), compress-bench, research + dictionary-fetch protocol design | MERGE next (after memtier push). Read `e2071ec` research doc for the "less overhead" verdict before summarizing to Jesse |
| sparq-fork | structural-fork | **~90% done, clean** — 4 commits to `3db0f8d`: `Graph::fork` O(delta) over Arc-shared storage, GraphApplier integration + threshold compaction, flat-read baseline, cache-fold fix. Agent's last words: "Now fix the bench per review findings (prepared query, derived ids)" | Relaunch FINISHER: fix bench per roborev findings, run fork-cost before/after (100k + 1M), flat-graph read-regression check, full gates (sparq-core/engine/serve), then merge |
| sparq-wavea | serve-wave-a | **code done, docs uncommitted** — 2 commits (`d85ab54` ring at/as_of + retention, `f1ae58d` server ?generation=N behind non-default `time-travel` feature); uncommitted: CHANGELOG.md, README.md, crates/sparq-server/README.md edits | Relaunch FINISHER: review + commit the doc edits, run gates BOTH feature states, then merge |
| sparq-proof | proof-trees | **nothing committed** — agent died in context-reading phase | Relaunch from scratch; full brief in transcript (search "Opt-in inference proof trees"). Key reqs: feature `explain` non-default, reuse incremental counting bookkeeping, ProofTree JSON/text, ZK-witness-friendly shape, differential + incremental-retraction tests, conformance 1637/0/17 unchanged |
| sparq-paths | path-pushdown | **COMPLETE, clean** — 2 commits to `2dbfca1`: bound-endpoint pushdown all 8 operators (~100,000× paired on bound-subject knows+), SCC cycle detection, in-loop budget, 270-case differential, conformance 1225/0/4 identical | MERGE (accept wasm +17,550B, note new baseline) |
| sparq-memtier | memory-tiering | merged into main locally (gate pending, see §1) | remove worktree + delete branch after push |

Merge order suggestion: memtier push → path-pushdown → custom-parsers D4 → serve-wave-a (after finisher) → structural-fork (after finisher) → proof-trees (when done).
Protocol: one merge at a time, full gate between, push, remove worktree. Agents never push/merge.

## 3. ZKP track (goal #6)

- `research/zkp-query-proofs-plan.md` **v2 PUSHED, AWAITING JESSE'S REVIEW** — named-graph credential
  model (proof statement S(R,Q,K,k): result holds over union of k undisclosed issuer-signed named
  graphs). New open questions Q3–Q13; Q3 (issuer-signature cryptosuite vs in-circuit standard-suite
  ~270–430k gates) is the big one. NO implementation until his feedback.
- `research/zkp-noir-context.md` PUSHED (recon of his DPhil work).
- **`research/zkp-performance-landscape.md` DOES NOT EXIST YET** — the synthesis agent was killed
  after its 9 sub-surveys completed but before writing. The sub-survey FULL TEXTS are in this
  session's transcript jsonl (path above) as task-notification results. Relaunch a synthesis agent
  that greps the transcript for these and writes the doc. Sub-surveys + one-line takeaways:
  1. Plonky3 vs Stwo — Stwo: production recursion (Mar 2026), Apache-2.0, wasm-SIMD backend, FibRace mobile evidence (<5s on phones); Plonky3: recursion unaudited, flagship user left.
  2. Metal/GPU economics — Apple-GPU MSM crossover ~2^20–2^22; CPU wins at credential scale.
  3. Jolt + folding — folding's ~10k-constraint/step overhead needs 5–30 predicates batched; final compression minutes-on-desktop/infeasible-on-phone (Cursive); Jolt: no wasm prover, no Poseidon2 inline, alpha.
  4. Binius/GKR — Binius dead as platform (Irreducible shut down 2025-11-12; binius64 community-alive); data-parallel GKR (Expander SIMD-GKR) structurally ideal for repeated small predicate circuits but AGPL + no browser story; ALL headline numbers vendor-only.
  5. FPGA/ASIC — nothing verifiably shipping; GPU commodity won; irrelevant at credential scale.
  6. UltraHonk vs Groth16 — UltraHonk 5–50× faster proving (independent Base benchmark), Groth16 ~7× cheaper verify + 128–256B proofs; bb.js <3s in-browser p256 on M1; Ultra-in-Ultra recursion gate count UNPUBLISHED; CHONK/ClientIVC not usable outside Aztec yet; snarkjs/rapidsnark are GPL-3.0.
  7. zkVMs + neutral benchmarks — PSE csp-benchmarks ON M1 (= Jesse's hardware): Binius64 41ms, Spartan2 73ms, Noir/bb 610ms vs RISC0 18.5s for same statement → purpose-built beats zkVM 1–3 orders; Google Longfellow-zk (Ligero, in Google Wallet, IETF draft) is THE production precedent for credential ZK; Microsoft Crescent (Groth16 over existing JWT/mDL) the other.
  8. ICICLE/sppark/cuZK — GPU E2E ~1–3× at 10^5–10^6 constraints, regression below; ICICLE GPU backends need commercial license for production; ICICLE repo quiet since Nov 2025.
  9. bb GPU / zkVM GPU — bb has NO shipped GPU prover (WebGPU MSM on unmerged feature branches); SP1/R0 real-time = 16+ datacenter GPUs; client-side consensus = CPU/WASM.
  Synthesis lens: workload = k≈2–5 graphs × tens of triples × 1–3 sigs, holder-side M1/phone/browser. Likely recommendation shape: stay Noir/UltraHonk near-term (formal-verification argument + browser numbers already adequate), hedge = GKR-class or Ligero/Longfellow-style for the hash-heavy commitment recomputation, revisit when CHONK opens up. But let the agent weigh it.

## 4. EC2 / Wikidata benchmark (goal #8)

- AWS profile `pss` re-authenticated and VERIFIED WORKING at handover. **No instances exist**
  (checked tag purpose=sparq-hw-validation just before stop — the Wikidata agent was killed before
  launching anything; $0 spent).
- Stage-1 brief (full text in transcript, search "Wikidata low-resource ingest curve"): ONE
  r8g.large on-demand + ~350GB gp3, tag everything purpose=sparq-hw-validation, NEVER touch
  i-090531b4ede8f2d3f, real latest-truthy slices 100M/300M/1B × {gz, zst}, external build, /usr/bin/time -v
  peak RSS, validate with COUNT queries, RDFox published-numbers context only, terminate + cost ledger,
  budget ~$3 of the $15 cap. → relaunch as-is.
- STILL ON JESSE: RDFox license (same-hardware head-to-head), sign-off on full-8B run (~$5–14).

## 5. Other open items

- **User-gated**: horizontal-scaling ADR sign-off (9 questions, research/adr-horizontal-scaling.md);
  ZKP plan v2 review; RDFox license; 8B-run budget.
- **Approved, not started**: SIMD/NEON sorted-intersection kernel + hash-probe prefetch (measure on
  EC2); adaptive re-optimization (measure-first); spill-to-disk joins (measure the cliff);
  memtier W1 per-perm tiering + W2 access counters implementation (design + binding config
  requirements in research/memory-tiering.md + research/memory-tiering-ADDENDUM-config-requirements.md);
  serving waves B (cache+tier0), C (scheduler), D (streaming — NOTE memtier found 1.75GB
  allocator-retained result materialization = the real serving memory cost; streaming serialization
  is the fix), E (benchmarks); fix pre-existing geo-feature test failure
  `unregistered_geof_iri_is_still_an_engine_error` (non-default feature combo, fails on parent of A4 too).
- **LAST per Jesse**: task #44 bindings parity (py ask/construct/describe, js ASK/named-graphs/
  streaming/incremental, fzstd JS decode + dictionary-fetch protocol — D4's protocol design feeds this).
- Standing rules: empirical honesty (measured REJECT > polite keep); strict subagent delegation,
  smallest context-independent deliverables; commits end `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`;
  CI <$5/mo; EC2 by tag only, ~$15 cap (≈$0 spent so far).
