<!-- [FABLE-5] sq-hmd7l.2 — per-axis gap record: full-text search. §3 filled by the
canonical wave-1 EC2 run (sq-hmd7l.26) from quiet-box envelopes with provenance.
Work-box readings are NON-canonical by construction and are never transcribed here. -->

# Gap record — full-text search (2026-07)

**Axis:** #13 in `research/comparative-benchmarking-everything.md` §5 (epic `sq-hmd7l`).
**Status:** latency axis MEASURED (canonical wave-1 run `sq-hmd7l.26`); BEIR quality axis still NOT-RUN — dedicated-box gather SCHEDULED as a committed one-command pair (see §4).
**Harness:** `scripts/bench/fts-same-box.sh` (this bead's deliverable, sq-hmd7l.2).
**Feeds:** the master table in `research/perf-dominance-gap-2026-07.md` (via `sq-hmd7l.27`).

## 1. Engines and honest scope

| Engine | Surface | Comparability |
|---|---|---|
| sparq-text | embedded BM25 inverted index + `text:` magic predicates | subject |
| Fuseki + jena-text (Lucene) | `text:query` over SPARQL — the only like-for-like FTS-over-SPARQL peer (`bench/competitors.json`) | latency axis, result-SET counts only |
| Lucene via Anserini | kernel BM25, **sub-component, not an RDF benchmark** — off the dashboard | IR-quality oracle only (BEIR) |

Scope caveats (pinned in `bench/fts/queries-jena-text/README.md`): result sets, never
ranking; `near_slop2` untranslated (Lucene unordered-slop vs sparq ordered-gap —
different result sets); mode asymmetry recorded (sparq embedded in-process vs jena-text
over loopback HTTP, its deployed surface). Solr/Elasticsearch are not SPARQL
competitors and stay off the table entirely.

## 2. Canonical protocol (design record §4)

1. Dedicated quiet EC2 box (tag `sparq-bench`, orphan-proof self-terminate), one engine
   active at a time, SAME corpus + SAME pinned 200-pair query set, best-of-N:

   ```sh
   CANONICAL=1 OUT_DIR=bench/canonical-competitor-results/<date>/ \
     TIMEOUT_S=900 scripts/bench/fts-same-box.sh          # latency axis (both engines)
   ```

2. **Oracle before stopwatch (INVARIANT):** per-workload hit counts are cross-checked
   engine-vs-engine (`count_crosscheck` in the envelope) and recorded **as returned**;
   a disagreement disqualifies that workload's comparative timing (`agree:false`) and
   is a recorded finding — never adjusted, never rerun-until-it-matches. The sparq side
   additionally rides the committed `bench/fts/expected.tsv` hard gate at the pinned
   corpus.

3. IR-quality axis (BEIR, gather-only — corpus not redistributable in-repo):

   ```sh
   cargo build -p sparq-text --release --example beir_text
   BEIR_CUT=scifact SPARQ_TEXT_BEIR=target/release/examples/beir_text \
     scripts/gather-competitors.sh --run --only lucene-anserini
   ```

   Both engines are scored by the one shared scorer against the same qrels; deficits
   (`1 - Recall@100`, `1 - nDCG@10`) land in the git-ignored gather results file, then
   here with provenance. The dedicated-box wrapper for this step is the committed
   launcher/instance pair `scripts/bench/canonical-beir-bench.sh` /
   `canonical-beir-gather-instance.sh` (sq-tvzyi — see §4).

4. Envelopes carry `canonical:true`, instance/commit/UTC provenance, statuses, and the
   crosscheck. A failed/absent competitor is a missing column + a re-run action, never
   a sparq win.

## 3. Results — latency axis (per-workload, best-of-N µs/query)

**CANONICAL — wave-1 run (`sq-hmd7l.26`).** Provenance: dedicated quiet box
`i-01a735e27b1764317` (c6i.4xlarge, eu-west-2, tag `sparq-bench`, self-terminated,
orphan-check clean), commit `cb1fc98c` (= `origin/main` `343aee547` + wave-1
runner-only fixes), envelope UTC `2026-07-10T00:44:50Z`, `CANONICAL=1`, N=100000
seed=0 corpus, 200 pinned pairs, best-of-3. Statuses: sparq ok, jena-text ok;
`pairs_pinned_check` ok; `doc_count_check` ok (100000 == N). Envelope:
`axis-results/fts/fts-n100000-20260710T004450Z.json` (launcher SSH pull + console).

| Workload | count crosscheck | sparq-text | jena-text | Verdict |
|---|---|---|---|---|
| `and_terms` | agree (52 = 52) | 11.9 µs | 3 177.9 µs | CLEARLY-AHEAD (~267×) |
| `or_terms` | agree (71 643 = 71 643) | 20.3 µs | 7 008.7 µs | CLEARLY-AHEAD (~345×) |
| `prefix4` | agree (6 727 493 = 6 727 493) | 3 414.5 µs | 254 655.0 µs | CLEARLY-AHEAD (~75×) |
| `phrase` | agree (6 = 6) | 1.4 µs | 2 735.3 µs | CLEARLY-AHEAD (~1954×, HTTP-floor caveat) |
| `near_slop2` | n/a (sparq 21 hits) | 1.4 µs | NOT-COMPARABLE (untranslated — semantics mismatch) | NOT-COMPARABLE |
| index build / corpus load | n/a (advisory) | 0.531 s | 2.1 s (GSP POST incl. Lucene indexing) | advisory only |

Mode-asymmetry caveat (recorded in the envelope, never adjusted): jena-text timings
include loopback HTTP round-trip (its deployed surface) — a few hundred µs floor. On
`phrase`/`and_terms` that floor is a visible share of the jena-text figure; even
subtracting a generous 500 µs floor the verdicts stay CLEARLY-AHEAD on every
translated workload. `prefix4`/`or_terms` are dominated by real result-set work.

## 4. Results — IR-quality axis (BEIR deficits, smaller is better)

> STILL NOT-RUN after wave-1 — the table below stays NOT-MEASURED (no quality claim
> either way) until a canonical gather-box run lands here with provenance. The
> bin-packed wave-1 runner (`scripts/bench/multi-axis-box.sh`) executes
> `fts-same-box.sh` — the latency axis only; the BEIR quality axis is a separate
> download-step gather (`scripts/gather-competitors.sh --only lucene-anserini`,
> sq-1fz0) whose heavy `pyserini`/`beir` provisioning was not part of the wave-1 box.
>
> SCHEDULED (**sq-tvzyi**): the gather is now a committed one-command pair —
> `AWS_PROFILE=pss scripts/bench/canonical-beir-bench.sh [<branch>]` launches a
> dedicated orphan-proof box (same rails as the materialize gather: terminate-on-
> shutdown, user-data watchdog, sentinel-gated teardown, console-recoverable
> envelopes) whose instance side (`canonical-beir-gather-instance.sh`) provisions
> pyserini+beir in a venv (JDK 21 for Anserini's Lucene), builds the `beir_text`
> example, and runs the §2.3 recipe per cut with sparq-text scored by the same
> scorer on the same qrels. Envelopes land in `bench/competitor-results/` +
> `gather-meta.json` (instance/commit/UTC/pip-version provenance); a maintainer
> transcribes reviewed deficits into the table below. Honest gap remains noted for
> the consolidation bead `sq-hmd7l.27` until that run executes.

| Cut | Metric | sparq-text deficit | Lucene/Anserini (oracle) deficit | Verdict |
|---|---|---|---|---|
| scifact | `1 - Recall@100` | NOT-MEASURED | NOT-MEASURED | NOT-MEASURED |
| scifact | `1 - nDCG@10` | NOT-MEASURED | NOT-MEASURED | NOT-MEASURED |

## 5. Verdict + fix plan

**Latency axis: CLEARLY-AHEAD on all four translated workloads** (~75× to ~1954×,
count crosscheck green on every row) — no BEHIND/PARITY rows, nothing for the
dominance fix queue from this axis. `near_slop2` stays NOT-COMPARABLE by design.

Remaining gap for `sq-hmd7l.27`: the BEIR IR-quality axis (§4) is unmeasured — a
quality (not latency) claim cannot be made either way until that gather runs.
Verdicts use the fixed vocabulary (CLEARLY-AHEAD / AHEAD-BUT-NOT-OOM / PARITY /
BEHIND / NOT-MEASURED / NOT-COMPARABLE); per the dominance mandate, every BEHIND or
PARITY row filed here must carry a root cause and an immediate P1 profiling-first
fix bead (consolidated by `sq-hmd7l.27`).
