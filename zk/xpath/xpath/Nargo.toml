[package]
name = "xpath"
type = "lib"
authors = ["jeswr"]

[dependencies]
# Vendored copies of upstream git deps (see ../vendor/*/VENDOR-PROVENANCE.md).
# Upstream pins were: ieee754 = jeswr/noir_IEEE754@v0.3.1, json_parser = noir-lang/noir_json_parser@main (floating).
ieee754 = { path = "../vendor/ieee754" }
json_parser = { path = "../vendor/json_parser" }
