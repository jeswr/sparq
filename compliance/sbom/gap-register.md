<!-- [OPUS-4.8] SBOM gap register — epic sq-toze / bead sq-toze.12. Every open gap carries a severity,
     a remediation plan, a target, and the tracking bead. No gap is papered over. -->
# SBOM + supply-chain — gap register

Severity: **P0** blocks a perfect score on a high-value framework; **P1** needed for a perfect score;
**P2** raises maturity / completeness. Each gap is honestly recorded with a remediation plan and a
`bd` bead under epic `sq-toze`. None is silently passed in the control table.

## Open gaps

| ID | Gap | Sev | Control(s) | Remediation plan | Target | Bead |
|---|---|---|---|---|---|---|
| **GS-1** | **Per-component Supplier Name slot (NTIA N1) not emitted (partial).** `cargo-cyclonedx` 0.5.9 leaves the dedicated `components[].supplier`/`.publisher` slot empty on **all 166** components (re-run probe 2026-06-15), so the literal NTIA "Supplier Name" *slot* is unpopulated per component. **However** the CycloneDX `author` field — the 1.3-era originator-identity carrier — *is* present on **144/166** components from each crate's `authors` metadata (the 22 without it: `sparq-core`/`sparq-engine`/`sparq-serve` + 19 deps), so the gap is **partial**, not total. (PURL + crates.io transitively identify the supplier-of-record, and the top-level supplier is in the VEX — so this is a *completeness*, not an *integrity*, gap.) | P2 | N1 | Either bump cargo-cyclonedx and emit CycloneDX 1.5/1.6 with `metadata.supplier` + per-component `publisher` from crates.io metadata (preferred), or post-process `scripts/gen-sbom-vex.sh` to inject the crates.io publisher per PURL. Add a probe assertion that the `supplier`/`publisher` slot is non-empty. | next SBOM tooling bump | `sq-toze.26` |
| **GS-2** | **No reproducible-build evidence (GX-8).** The SBOM↔binary link is asserted via SLSA provenance (SIG-1) + the embedded cargo-auditable manifest (DEP-5), but an auditor cannot independently rebuild a bit-identical binary from the SBOM. Higher SLSA levels + CRA integrity want this (or an honest "not reproducible because…"). | P2 | INT-3 | Produce reproducible-build evidence (pinned toolchain + `--locked` + deterministic flags) OR a documented honest statement of which inputs (timestamps, `RUSTFLAGS=-Ctarget-cpu`, build-path) prevent reproducibility. Shared with the SLSA + CRA worktrees. | aligned with `slsa` worktree | `sq-toze.9` (GX-8) |
| **GS-3** | **No dedicated npm/JS-lockfile SBOM** for the WASM client (`crates/sparq-wasm`, `js/`). The Cargo tree + npm-ecosystem Dependabot cover the deps, but there is no CycloneDX SBOM for the published npm package surface specifically. | P2 | scope (WASM client) | Add a per-release CycloneDX SBOM for the npm package (e.g. `@cyclonedx/cyclonedx-npm`) attached to the JS release. | with next JS release wiring | `sq-toze.27` |
| **GS-4** | **RESOLVED ([OPUS-4.8], sq-toze.28).** ~~Generated SBOM is CycloneDX 1.3~~ (was: cargo-cyclonedx default 1.3 while the VEX is 1.5 — mixed spec versions are valid but 1.5/1.6 carries richer slots). **Fixed:** both SBOM call-sites now pass `cargo cyclonedx … --spec-version 1.5` (native; cargo-cyclonedx 0.5.9 accepts `1.3`/`1.4`/`1.5`), so the published SBOM and the VEX are both CycloneDX **1.5**. `scripts/sbom-normalize.jq` additionally populates the 1.5-only `metadata.lifecycles` slot with the single honestly-assertable phase, `build` (the BOM is produced from the fully-resolved dependency tree during the build). Validated against the official CycloneDX 1.5 JSON schema (both `sparq-cli`/`sparq-server` SBOMs: VALID, lifecycles present, 0 host-path leaks, dependency graph fully resolves, normalizer idempotent). 1.6 not adopted: the pinned tool tops out at 1.5, and no 1.6-only field could be populated honestly without fabrication. | P2 | CDX-3 | **DONE** — `--spec-version 1.5` in `scripts/gen-sbom-vex.sh` + `supply-chain.yml#sbom`; `metadata.lifecycles` via `scripts/sbom-normalize.jq`. | landed | `sq-toze.28` |
| **GS-5** | **VEX ↔ deny.toml sync is enforced by comment + manual inspection, not CI.** A future advisory added to `deny.toml [advisories].ignore` without a matching VEX entry (or vice-versa) would not be caught automatically. (Currently **in sync** — verified this branch.) | P2 | VEX-3 | Add a CI check (in `supply-chain.yml`) that fails if the RUSTSEC id set in `deny.toml [advisories].ignore` ≠ the VEX `vulnerabilities[].id` set. Lands test-first. | next supply-chain.yml edit | `sq-toze.29` |
| **GS-6** | **RESOLVED ([OPUS-4.8], sq-toze.30 + sq-uujh).** ~~SBOM root identity leaks the absolute build-machine path.~~ cargo-cyclonedx 0.5.9 *raw* output stamped the absolute build dir into the root `metadata.component.bom-ref` (`path+file:///<abs-path>/crates/sparq-server#0.1.0`, root `purl` = `…?download_url=file://.`) and every workspace-member ref, so a published SBOM would have carried the **CI runner's absolute path**. **Fixed:** the publication path now post-processes every shipped SBOM through `scripts/sbom-normalize.jq` — invoked from `scripts/gen-sbom-vex.sh` (the two released-binary SBOMs) and `supply-chain.yml#sbom` (every uploaded `*.cdx.json`) — which rewrites each `path+file://…#<ver>` ref to the canonical host-independent `pkg:cargo/<name>@<version>` form and strips the whole `download_url=file://…` purl qualifier **plus the trailing build-target `#src/…` subpath** (extended in sq-uujh — see GS-7), rewriting the dependency graph (`bom-ref` + every `ref`/`dependsOn` edge + nested build-target sub-components) in lock-step so internal refs still resolve. Deterministic + idempotent; both call-sites also hard-fail if any `path+file://`/`download_url=file://`/`/home/` survives. Verified locally (0 leaks, valid CycloneDX, all dep refs resolve, idempotent). *Residual (not blocking):* raw `cargo cyclonedx` still emits path refs upstream — only the post-processed publication output is sanitised. See `evidence.md §3`. | P2 | INT-1/INT-2 (hygiene) | **DONE** — `scripts/sbom-normalize.jq` + wired into `gen-sbom-vex.sh` & `supply-chain.yml#sbom`. (Upstream cargo-cyclonedx fix deferred → new bead.) | landed | `sq-toze.30` |
| **GS-7** | **RESOLVED ([OPUS-4.8], sq-uujh).** ~~Workspace-member / build-target purls were not fully canonical.~~ After the sq-toze.30 normalizer, every workspace-member component purl was canonical `pkg:cargo/<name>@<version>` **except the root build-target sub-components** (bin/lib targets), whose purls retained a host-derived subpath fragment: `pkg:cargo/sparq-cli@0.1.0#src/main.rs`, `pkg:cargo/sparq-server@0.1.0#src/{lib,main}.rs`. `canon_purl` stripped the `?download_url=file://…` qualifier only *up to* the `#`, leaving the fragment — so these were the **only** non-canonical purls in the SBOM and produced a purl↔`bom-ref` mismatch on those rows (the `bom-ref` was already canonicalised). **Fixed:** `canon_purl` now drops the download_url qualifier together with any trailing `#subpath` (`sub("[?&]download_url=file://.*$"; "")`), gated on the workspace-local download_url marker so registry purls are byte-for-byte untouched. Verified: all member + build-target purls canonical, registry purls unchanged, idempotent, both SBOMs VALID against the official CycloneDX 1.5 schema. **Upstream:** raw cargo-cyclonedx 0.5.9 still emits these path-refs/subpath-purls — the proper fix belongs upstream; file an issue/PR against `CycloneDX/cyclonedx-rust-cargo` to emit canonical `pkg:cargo/<name>@<version>` purls for workspace/path members (tracked under sq-uujh). | P2 | INT-1/INT-2 (hygiene) | **DONE** — `scripts/sbom-normalize.jq` `canon_purl` extended; upstream issue/PR pending. | landed | `sq-uujh` |

## Notable NON-gaps (recorded so the auditor does not re-open them)

These were candidate gaps in the original cross-cutting register that are now **closed** — cite as
evidence, do not re-propose:

| Was | Now | Evidence |
|---|---|---|
| **GX-1** — degraded advisories PR-gate (`continue-on-error`) | **Closed** (sq-toze.2). Advisories check is **GATING**; CVSS-4.0 blocker (sq-q8de) resolved. | `supply-chain.yml#audit` "cargo-deny check (advisories) — GATING"; `deny.toml` fail-closed. |
| **GX-2** — no checked-in / per-release SBOM with VEX | **Closed** (sq-toze.3). Per-release SBOM + version-stamped VEX, attested + attached. | `scripts/gen-sbom-vex.sh`; `supply-chain/vex.cdx.json`; `release.yml#sbom`, `#release`. |
| **GX-3** — no `.well-known/security.txt` (RFC 9116) | **Closed** (sq-toze.4). | `.well-known/security.txt` (Contact + Policy + Expires 2027-06-15). |
| **GX-7** — no cargo-auditable / cargo-vet | **Implemented & verified; bead `sq-toze.8` CLOSED.** Both are **wired**: cargo-vet is a GATING CI job (operating-verified on every PR), cargo-auditable is used in release + Docker builds. The tracking bead `sq-toze.8` is **closed** (verified in `.beads/issues.jsonl`). | `supply-chain.yml#vet`; `release.yml#package` + `Dockerfile:L70`. (Bead `sq-toze.8` = `closed`; no further close-out action needed.) |

> **Honesty note for the auditor:** the SBOM posture is strong — of the **31 control rows**, **22 are
> implemented & verified, 6 are audit-ready** (release-gated: config-verified but not yet
> operating-verified, since no `v*` release/tag/attestation exists — verified 2026-06-15), and **2 are
> gaps** (N1, INT-3 — CDX-3 now met). Of the original six open gap items (GS-1..6), **GS-6 (abs-path leak)
> is RESOLVED** ([OPUS-4.8], sq-toze.30 — SBOM publication normalized via `scripts/sbom-normalize.jq`)
> and **GS-4 (spec version) is RESOLVED** ([OPUS-4.8], sq-toze.28 — SBOM now emitted as CycloneDX **1.5**,
> matching the VEX, with `metadata.lifecycles` populated), leaving **four open**, all **P2 completeness /
> maturity / hygiene** items (partial supplier field, reproducibility, JS SBOM, drift automation);
> **none** is a P0/P1 integrity or transparency hole, and **none** is overclaimed in
> `controls/sbom.md`. Note GS-3 is a scope item with no control row and GS-5 sits behind the
> otherwise-met VEX-3, so the gap *items* ≠ the 3 gap *rows*. The one item that could look like an open
> gap — GX-7 (cargo-auditable/cargo-vet) — is **implemented & verified** and its bead `sq-toze.8` is
> **closed**; this register cites it as evidence, not a gap.
