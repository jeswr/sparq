<!-- [OPUS-4.8] sq-toze / sq-toze.20 — Cryptographic-review gap register. Honesty-critical.
     Authored by Opus 4.8 while Fable 5 unavailable — re-review when Fable returns. -->

# Cryptographic review — gap register

Open gaps for the cryptographic-review framework, severity-ordered. **The headline is not a
list of small holes — it is a single architectural truth (CR-G1) that gates every production
ZK/MPC security claim.** Each gap names its remediation + a `bd` bead (epic `sq-toze`; the ZK
remediation itself lives under epic `sq-1s2`).

---

## HEADLINE — read this before anything else

<!-- [OPUS-4.8] reconciled with post-remediation re-audit (sq-gbp4); see ZK-verdict cross-ref sweep -->
> ### sparq's bespoke cryptography is NOT externally verified.
>
> - The **v1 ZK verifier** (`sparq-zk` / `sparq-zk-compose`) is published in `SECURITY.md`
>   (§"`sparq-zk` and `sparq-zk-compose` — ZK verifier: remediated, but NOT externally
>   audited") as **remediated but externally unaudited** — the original audit found it BROKEN,
>   the `sq-1s2` binding layer has landed, and the internal re-audit found it *"sound as landed
>   for the threat model the prior audit assumed,"* **but** `SECURITY.md` still directs a
>   relying party **not** to present a *"verified"* result as a production-grade guarantee until
>   the external cryptographer audit completes. That re-audit is a **single-model, internal,
>   self-review** (Opus 4.8, Fable unavailable, read-only, crypto-chain forge tests
>   `#[ignore]`d) — **necessary but NOT sufficient** to support a production soundness claim.
> - **`sparq-mpc`** provides **NO confidentiality, correctness, attestation, or
>   malicious-security guarantee** — the malicious-security / collaborative-proof core is
>   **DEFERRED** behind honest `NotYetImplemented` stubs.
> - **An external accredited-cryptographer audit (`CR-G1` / bead `sq-qhy4`, P0) is REQUIRED
>   before ANY ZK/MPC security, privacy, integrity, or attestation property may be relied
>   upon in production.** No internal document — including the favourable re-audit, this
>   compliance slice, or any CDMC/maturity score — substitutes for it.
>
> Containment (honest mitigant, NOT a substitute for the audit): all three bespoke-crypto
> crates are `publish = false` and excluded from `sparq-wasm`, so a downstream consumer of the
> published artifacts cannot reach this code by default (CR-13). The gap is real but
> *contained to an opt-in research surface*.

---

## Gap table

| ID | Gap | Severity | Disposition | Remediation | Bead |
|---|---|---|---|---|---|
| **CR-G1** | **No external accredited-cryptographer audit** of the ZK verifier binding layer, the Schnorr-over-Baby-JubJub issuer-signature scheme, the Noir circuits, or the composition seam. All soundness assurance is internal self-review. | **CRITICAL** | **EXTERNAL-REQUIRED** (out of agent scope) | Commission an external accredited cryptographer to re-run the 12-finding adversarial pass + (ideally) a machine-checked formal verification of `reconstruct_public_inputs` ↔ circuit-`main` correspondence. This slice + `research/zk-soundness-audit.md` + `research/zk-verifier-reaudit.md` + `forge_gates.rs` are the evidence pack. **Until it closes, `SECURITY.md`'s conservative posture — remediated but externally unaudited, "do not present a verified result as a production-grade guarantee until `sq-qhy4` completes" — is the relying-party truth.** [OPUS-4.8] | `sq-qhy4` (P0) |
| **CR-G2** | **Crypto-chain forge tests are `#[ignore]`d** out of default CI (need nargo/bb), so closure of the 12 findings rested on code-reading + 2 anchors, not default-CI execution. | HIGH | **ADDRESSED (verify before relying)** | A toolchain-gated CI lane now runs the `#[ignore]`d `forge_pubinput_*` + real-bb e2e suite on zk changes (`.github/workflows/zk-toolchain.yml`, `zk forge + anchor suite (nargo/bb)`). Confirm the lane is green and required-gated as part of CR-G1's external pass. | `sq-f9tl` (**closed**) |
| **CR-G3** | **Public-input empirical anchors missing for `filter_f64_d*` + k=2 scan members** — only `filter_int_d1` + `scan_k1_n16_r4` had non-circular golden vectors; the rest relied on layout reasoning, so a toolchain bump could silently drift their serialization. | HIGH | **ADDRESSED (verify before relying)** | Probe-captured anchors for the f64 + k=2 members landed under the same toolchain-lane bead. Verify the anchors exist + the lane recaptures-and-compares them. | `sq-f9tl` (**closed**) |
| **CR-G4** | **No FIPS 140-3 validated module; FIPS posture now stated** for an operator with a FIPS deployment constraint. | LOW | **ADDRESSED → AUDIT-READY** (honest negative claim) | A standalone FIPS posture statement now documents the negative claim explicitly: [`fips-posture.md`](./fips-posture.md). It lists the non-FIPS-approved ZK primitives (BN254 / Poseidon2 / Schnorr-over-Baby-JubJub), notes SHA-256 release digests are FIPS 180-4 but used *outside* any CMVP module, records the `publish = false` / non-default containment, and gives FIPS-constrained-operator guidance — **no FIPS claim, no CMVP claim**. A short FIPS note is also added to the crypto crate docs (`crates/sparq-zk-compose/README.md`, `crates/sparq-zk/src/lib.rs`, `crates/sparq-mpc/src/lib.rs`). A `SECURITY.md` cross-ref is governance-owned and deferred to a follow-up bead (not made here). | `sq-cu32` (**landed**) |
| **CR-G5** | **No constant-time / side-channel analysis** of the secret-bearing paths (issuer signing, MPC share ops); no `dudect`/`ctgrind` measurement; arkworks BN254 field ops not asserted CT. | P2 | **ADDRESSED-by-analysis** (source-level only; an instrumented `dudect`/`ctgrind` pass folds into CR-G1) | A **source-level** constant-time / side-channel review now exists: [`side-channel-analysis.md`](./side-channel-analysis.md). Per-component verdicts: the verify path carries no secret (public data); the MPC paths open only masked products / the verdict bit (operands never reconstructed); Poseidon2 + the MPC bit-circuit are fixed-round with no secret-dependent branch/index; the masking RNG is effectively-CT-in-expectation. Present exposure is **LOW by architectural placement, NOT by constant-time primitives**. **Three concrete fix recommendations were bead-tracked from the analysis:** `sq-u8a8` (adopt `subtle`/`zeroize`), `sq-7ltf` (latent non-CT `Fp::pow`, guarded today only because its sole caller passes the public `P-2`), `sq-8jv7` (Schnorr signing uses arkworks scalar ops not asserted CT; issuance-side only). **`sq-u8a8` is now LANDED (hygiene only, [OPUS-4.8], Fable re-review pending):** `zeroize` zeroizes the issuer/holder `SecretKey` scalar, the cleartext session MAC key `α`, the secret-shared `[α]` / `[α·x]` share vectors, and gives `Fp` / `Share` a `Zeroize` impl, all on drop; `subtle::ConstantTimeEq` is adopted as a defensive PRIMITIVE (`SecretKey::ct_eq`, `Fp::ct_eq`). **Per the analysis, NO existing `==` was replaced** — every comparison on the live protocol paths is over PUBLIC / opened values (verify equation, masked product `==0`, verdict bit, public threshold bits), so CT-eq is added for future secret-vs-secret use, not to fix a present leak. All disclosure-minimisation + MAC-soundness + hidden-join + tamper-detection tests pass UNCHANGED (proving protocol behaviour is identical). **`sq-19ej` is now ADDRESSED ([OPUS-4.8], Fable re-review pending):** the one secret byte buffer `sparq-mpc` controls — the masking-CSPRNG seed — is now scrubbed. `SecureRng::from_os` pulls the 32-byte ChaCha seed into a `zeroize::Zeroizing` buffer and wipes it on scope exit (previously `from_os_rng()` routed the OS seed straight into `ChaCha20Rng` with no sparq-side copy to scrub), and `Drop for SecureRng` best-effort-scrubs the most-recently-buffered keystream block. **Residual (was documented NOT-scrubbed; now CLOSED by `sq-it50`):** under `rand_chacha` the inner `ChaCha20Rng` key schedule (recoverable via `get_seed()`) was not scrubbable in place — `rand_chacha` exposes no `zeroize` feature and no `Drop`/`&mut`-key accessor, **re-verified at the latest published `0.10.0` (Feb 2026)** as well as the pinned `0.9`; the value is stack-allocated so there is no heap alloc to zero. sq-19ej therefore documented this honestly as NOT-scrubbed rather than fake a scrub. **`sq-it50` is now ADDRESSED ([OPUS-4.8], Fable re-review pending) and closes that residual:** the production `SecureRng` no longer wraps `rand_chacha` — it now backs onto a minimal, **sparq-owned** ChaCha20 keystream generator (`crate::chacha::ChaCha20Csprng`, private to the crate) whose full 16-word block state (the expanded key, the block counter, and the cached 64-byte keystream block) is `#[derive(ZeroizeOnDrop)]`. Dropping `SecureRng` drops the inner cipher, zeroizing every secret word; there is no sparq-reachable copy of the key material that survives drop and no `get_seed()`-style accessor. The owned cipher is **correctness-pinned to two independent RFC 8439 known-answer vectors** (§2.3.2 block function + §2.4.2 keystream) and the scrub is proven by test (`key_schedule_is_zeroized_on_drop` asserts the exact `zeroize()` that `Drop` runs wipes the key words). The now-unused direct `rand` dependency is dropped; and as of the rand-ecosystem 0.10 upgrade (`sq-8xug`, [OPUS-4.8]) the `rand_chacha` dependency is dropped too — `rand_core` 0.10 removed `OsRng`, so `from_os` now draws the 32-byte seed directly from `getrandom::fill` (the same OS-entropy source `OsRng` wrapped). **This is hygiene, NOT a protocol/soundness change:** the masking RNG is still a ChaCha20 CSPRNG keyed by fresh OS entropy drawn as uniform `F_p` elements; `SecureRng` is never seeded deterministically (only the test-gated `InsecureTestRng` is), so no test or protocol depends on a specific seed→stream mapping. RNG behaviour for the protocol is identical — all 297 MPC tests pass UNCHANGED in both feature states. **`sq-7ltf` is now ADDRESSED (latent-gap close, [OPUS-4.8], Fable re-review pending):** the non-constant-time `Fp::pow` (square-and-multiply branching on the exponent bits) was split — `Fp::pow_vartime` keeps the variable-time path under an explicit **public-exponent contract** (its only caller, the robust Berlekamp–Welch decoder, raises a public point to the public error-count `e`), and a new branchless fixed-64-iteration `Fp::pow_ct` (conditional multiply via `subtle::ConditionallySelectable`) is constant-time in the base by construction. **`Fp::inv` now routes through `pow_ct`**, so field inversion (Lagrange / Gaussian elimination) is constant-time in the inverted value rather than safe only by current-caller convention. The exponent `P−2` is a public constant so no present timing was secret; this closes the latent gap for any future secret-value inversion. **No arithmetic changes** — `pow_ct` ≡ `pow_vartime` proven by test (`field.rs`), and all 270 MPC tests pass UNCHANGED. **This is NOT a soundness fix:** the ZK verifier's posture is unchanged by it — remediated but NOT externally audited, no production guarantee until the external sign-off (`SECURITY.md` / CR-G1, `sq-qhy4`) — and `sparq-mpc` carries no security guarantee. **`sq-8jv7` is now ADDRESSED (honest hardening + documented residual, [OPUS-4.8], Fable re-review pending):** the Schnorr SIGN path (`sparq-zk/src/sig.rs`) had ONE secret-dependent branch in code we own — `derive_nonce`'s degenerate-`k` guard (`if k.is_zero() { refold } else { k }`, branching on the secret nonce). It is now **branchless**: the re-fold candidate is ALWAYS computed and `subtle`-selected (`ConditionallySelectable` byte-wise, keyed on a `ConstantTimeEq` zero-test), so our emitted control flow is data-independent of the secret nonce. **The irreducible residual is DOCUMENTED, not claimed-CT:** the Baby-JubJub scalar multiplication `R = G·k` and the scalar arithmetic `s = k + e·sk` use arkworks (`ark-ed-on-bn254`/`ark-ff`) ops sparq does **NOT** assert are constant-time, and cannot make CT without replacing the curve implementation (out of scope) — a module CONSTANT-TIME POSTURE note in `sig.rs` states this plainly. **Why LOW:** signing is **issuance-side** (trusted environment); the relying party only calls `verify` (public data). A constant-time scalar-mul becomes load-bearing only if signing moves to an exposed/online surface (e.g. the in-circuit hidden-key upgrade `sq-1s2`); a curve/dep swap for true CT scalar-mul is the deferred follow-up (**`sq-j3b9`, now PARTIALLY delivered — see below**). **No protocol change** — the branchless guard is byte-identical to the old select for all reachable inputs (`k == 0` is ~2^-251), proven by test (`branchless_nonce_matches_first_fold_and_signs_stably`); ALL 27 `sig` tests + the full `sparq-zk` suite pass UNCHANGED (signatures still verify). **Limitation (honest):** source-level reading, not an instrumented timing study (NON-CANONICAL timing); arkworks field-op timing is a black box; the crate is research-grade + externally unaudited (CR-G1, `sq-qhy4`). **`sq-j3b9` is now PARTIALLY ADDRESSED — the scalar-mul SHAPE, explicitly NOT the field ops ([OPUS-5], crypto re-review pending):** the two SECRET-scalar multiplications (`pk = sk·G` in `SecretKey::public_key`, `R = G·k` in `sign_deterministic`) no longer call arkworks' generic `Group::mul_bigint`, whose double-and-add leaked the secret scalar twice over — the `without_leading_zeros` short-circuit made the loop trip count a function of the scalar's bit LENGTH, and `if b { res += self }` made per-iteration work a function of each individual scalar BIT (the textbook square-and-multiply channel, and the strongest secret-dependent signal on the signing path). They now use a sparq-owned fixed-width double-and-ALWAYS-add ladder (`sparq-zk/src/ct.rs::mul_ct`): a constant `MODULUS_BIT_SIZE` trip count regardless of the scalar and identical per-iteration work regardless of the bit, with the addend selected between the base point and the twisted-Edwards affine identity `(0, 1)` by pure field arithmetic — no branch, no table index. Adding the identity is *correct*, not merely harmless: Baby-JubJub's addition law is complete and `ark-ec` implements the unified `E^e` addition (the same property `mul_bigint` already relied on). **What sq-j3b9 explicitly does NOT close — the actual curve/dep swap, still OPEN:** every operation still bottoms out in arkworks `ark-ff` field arithmetic (Montgomery reduction ending in a conditional subtraction) that sparq does **not** assert is constant-time; and `s = k + e·sk` is untouched arkworks scalar arithmetic (a fixed two-operation sequence with no loop or branch, so it carries no square-and-multiply structure, but it inherits the same field-op residual). **The "NOT asserted constant-time" posture and the LOW rating are UNCHANGED** — hardening the ladder's shape does not upgrade the claim — and no instrumented `dudect`/`ctgrind` pass has been run (still folds into CR-G1). The public-data `verify` path deliberately KEEPS the faster variable-time arkworks multiplication (no secret on it). **No protocol/value change:** the ladder is value-identical to the multiplication it replaces, so every derived key and signature is byte-identical and already-issued credentials stay valid — pinned by a randomized differential against the arkworks reference (`ct.rs`; covers `k = 0`, `k = r−1`, and a non-generator base) plus a call-site equivalence test in `sig.rs`. Adds **no dependency**. | `sq-egx6` (P2, analysis done); `sq-u8a8` (**LANDED**); `sq-19ej` (**ADDRESSED**); `sq-it50` (**ADDRESSED** — owned ChaCha key schedule now ZeroizeOnDrop, closes the sq-19ej residual); `sq-7ltf` (**ADDRESSED**); `sq-8jv7` (**ADDRESSED**); `sq-j3b9` (**PARTIAL** — scalar-mul shape hardened; arkworks field-op curve/dep swap still open) |
| **CR-G6** | **Residual ZK privacy / binding deferrals** (not soundness breaks): in-circuit salt binding not done (salt verified verifier-side-clear); per-graph salt + list-IRI/version disclosed in the clear (linkability handles); `HolderPop` not yet credential-bound (a trusted holder could present another trusted holder's credential). | MEDIUM (privacy) | **OPEN** (tracked) | The in-circuit privacy upgrades. These are explicitly documented deferrals (`research/zk-verifier-reaudit.md` NEW-1/NEW-2), **not** soundness re-openings — a revoked/forged credential still fails; an untrusted holder still fails. | `sq-hyhj`, `sq-93h`, `sq-i1dt`, `sq-42e3` (all open, under `sq-1s2`) |
| **CR-G7** | **`sparq-mpc` malicious security / collaborative-proof (M4, Q1) deferred** — no malicious-security, no distributed-signature-over-secret-shared-witness. | HIGH (for any MPC reliance) | **NOT-SOUND (research-only)** / deferred | Out of scope for this certification epic; the MPC build-out is tracked separately. The honest statement (`SECURITY.md` + `sparq-mpc` manifest) must accompany any mention of MPC. | `sq-bjl` (deferred SPIKE), `sq-34ml` (M4-v1 prerequisites), `sq-ox16` (covert/PVC model) |
| **CR-G8** | **DUAL-LEAF value+lexical term/literal encoding (`VALUE_HOOK` + lexical-identity hash) is UNAUDITED and not yet implemented, and it REMOVES an invariant the current circuit enforces.** The finalized design (`research/zk-field-native-encoding.md`, the `#769` dual-leaf decision, superseding the value-only draft PR #765) commits `Poseidon2([value_component, lexical_component, TYPE_CODE])`: a per-datatype numeric handle for cheap value-FILTERs AND a carried lexical-identity hash (`blake3` of the canonical token) for term identity. **Adversarial review (`research/zk-dual-leaf-issuer-desync-review.md`, PR #793) found, against the actual code, that the current `filter_int`/`filter_signed`/`filter_decimal`/`filter_float` members enforce IN-CIRCUIT, against arbitrary committers including a malicious trusted issuer, the invariant INV-VL = "compared value equals parse(committed lexical)" — value and binding derive from ONE witnessed digit set (`filter_int.nr:67-92`, `filter_signed.nr:150-180`). The dual leaf witnesses the value handle and the lexical hash INDEPENDENTLY, with nothing tying them, so it REMOVES INV-VL.** Three NEW obligations follow: (i) the value-FILTER lane regresses from machine-enforced to ISSUER-HONESTY-trusted for value↔lexical agreement (a malicious *trusted* issuer can commit one signed credential answering a value question as `18` and a sameTerm/DISTINCT/join question as `5` — impossible today; no untrusted party can exploit it; the host-side same-leaf co-binding at ingest, fail-closed, makes honest sparq ingest unable to desync); (ii) the dual-leaf FILTER member MUST INSTANTIATE (not merely name) an explicit in-circuit range-decomposition of `VALUE_HOOK` (B1, no modular wrap) and a canonical-form bind (B4, no second encoding) — else in-range-ness and single-encoding ALSO downgrade from prover-side circuit guarantees to issuer/ingest assumptions (a strictly larger escalation); (iii) `VALUE_HOOK` is MANY-TO-ONE on the term for double/float (`-0.0`/`+0.0` equal, `tests.nr:379-380`; NaN payloads) and decimal (`"5.0"`/`"5.00"` at fixed `FD`), so `value_component` is a term-identity HAZARD and reject-list (v) ("no identity operator reads `value_component`") MUST be STRUCTURALLY enforced, not left as prose — term-identity preservation is CONDITIONAL on that, not a property of the encoding. **None of this is established — it is an OPEN obligation registered up front (per the maintainer's direction to introduce the encoding at research grade and flag it for the audit), never a resolved or safe claim.** [OPUS-4.8] | HIGH | **OPEN / EXTERNAL-REQUIRED** (forward obligation for a greenlit-but-unimplemented change) | The external accredited-cryptographer pass (CR-G1 / `sq-qhy4`) MUST verify, before any production reliance: (1) the relocated operand binding (§8 A1/A4) still binds the **scan-committed** triple, AND the SAME range-decomposed `VALUE_HOOK` feeds both the binding and the typed comparison; (2) the B1 range-decomposition and the B4 canonical-form bind are present as IN-CIRCUIT constraints — OR the docs/SKILL/README state that in-range single-encoding now rests on issuer honesty too; (3) reject-list (v) is STRUCTURALLY enforced so NO identity-sensitive operator (scan-row equality, `join_eq`, `DISTINCT`, `sameTerm`) can read `value_component`, AND the many-to-one-ness (IEEE signed-zero/NaN; decimal fixed-`FD`) cannot leak onto any identity path; (4) the removed-INV-VL value↔lexical residual is bounded to a malicious *trusted* issuer (no untrusted party can forge a desynced leaf), and canonical-issuance conformance is named as the precondition that restores INV-VL; (5) the `xsd:double`/`float` IEEE-bit ingest canonicalisation (signed-zero/NaN rule included) is total and correct. Until that pass closes, the §8 relocation is preserved-by-construction-only (an obligation, not a fact); the behavioural caveats land WITH the implementation (`sq-j506`). [OPUS-4.8] | `sq-qhy4` (P0, external) / design `sq-1s2.1.1` (#769); impl `sq-j506` (audit-gated) |
| **CR-G9** | **The commitment-method × circuit × signature compatibility matrix is a new fail-closed soundness surface.** The maintainer-greenlit (#769) configurable-commitment program makes the commitment method a SELECTABLE, recorded property (string-canonical / dual-leaf / value-only over the `zk:scheme` slot — the registry LANDED in #891 / `sq-zzxt`) and will add a pluggable signature trait (`sq-1hsl`) and value-bearing circuit members (`sq-xojl`). Selectability means a verifier MUST refuse any `(zk:scheme, CircuitId)` or `(zk:scheme, zk:cryptosuite)` pair OUTSIDE the legal matrix (design `research/zk-configurable-commitment-design.md` §3.1): a value-bearing FILTER against a method with NO value handle, an identity operator (`scan`-row eq / `join_eq` / `DISTINCT` / `sameTerm`) routed at a many-to-one `value_component`, or an unknown/mismatched method-or-cryptosuite IRI **defaulting instead of rejecting** are each a soundness break. **Partially seamed today:** the `CommitmentMethod` registry IS fail-closed on an unknown `zk:scheme` IRI (#891 — `commit::CommitmentMethod::from_scheme_iri` returns `None`, `RegistryEntry::method()` returns `None`, NEVER a default; the value-only IRI is rejected unless the `commitment-value-only` feature is on — tests `method_parse_is_fail_closed_on_unknown`, `value_only_iri_is_rejected_without_the_feature`), but the `(method, circuit)` dispatch (`sq-cfmv`) and the value-bearing members (`sq-xojl`) the matrix governs are **NOT yet implemented**. This is config-only plumbing — **no leaf-shape / circuit / encoding change has landed** — so NO soundness property of the matrix is established; it is an OPEN obligation registered up front per the maintainer's research-grade direction, never a resolved or safe claim. Complements CR-G8 (which governs the dual-leaf encoding's removed INV-VL invariant); CR-G9 governs the SELECTION surface that decides which encoding+circuit is legal. [OPUS-4.8] | HIGH | **OPEN / EXTERNAL-REQUIRED** (forward obligation; the registry half is in place, the dispatch half is unimplemented) | The external accredited-cryptographer pass (CR-G1 / `sq-qhy4`) MUST verify, before any production reliance: (1) the `(method, circuit, signature)` compatibility matrix is enforced **fail-closed end-to-end** — NO proof verifies under a `CircuitId` illegal for its recorded `zk:scheme`; (2) NO identity-sensitive operator can read a `value_component` on a dual-leaf or value-only graph (reject-list (v) is STRUCTURALLY enforced by the resolver, not prose — carried from CR-G8 obligation (3)); (3) an unknown / mismatched commitment-method or `zk:cryptosuite` IRI REJECTS rather than silently defaults (the registry already does this for the method axis; the auditor confirms the dispatch + signature axes do too); (4) value-only graphs reject identity ops at plan time rather than answer them on a collapsing value handle. Design records: `research/zk-configurable-commitment-design.md` (the program, §8 CR-G9 proposal) + `research/zk-configurable-commitment-security.md` (the per-method security-properties write-up, `sq-pkrl`). [OPUS-4.8] | `sq-qhy4` (P0, external) / design `sq-pkrl` (this register row) + `sq-1s2.5`; dispatch `sq-cfmv` + members `sq-xojl` (audit-gated) |

---

## What is explicitly NOT a gap (and must not be re-raised as one)

To prevent re-litigation, these are the controls already in place — cite them as evidence, do
not list them as gaps:

- The 12 historical CRITICAL/HIGH findings each have an internal **CLOSED** disposition with a
  reject-path + (mostly) a forge test — `research/zk-verifier-reaudit.md` §"Per-finding
  dispositions" + the 1:1 map. (Closure is *internal*; CR-G1 is the external gate over all of
  it.)
- The forge-and-verify negative suite (`forge_gates.rs`, `sq-ajl`), the differential fuzzer
  (`differential_fuzz.rs`, `sq-61g`), the gate-count ratchet (`gate_count.rs`, `sq-c5f`), and
  the pinned toolchain lane (`zk-toolchain.yml`) all exist and are cited in `controls.md`
  CR-6/7/8/9.
- Sigstore build-provenance attestation, SHA-256 digests, RDFC10, and OS-CSPRNG sourcing are
  **Tier-A sound** (CR-11) — they are not part of the unsound surface and need no remediation.
- Containment (`publish = false` + wasm exclusion, CR-13) is a mitigant, not a gap.

---

## Severity rationale (honesty calibration)

- **CR-G1 is CRITICAL not because the verifier is known-broken today** (the internal re-audit
  argues it is sound as landed) **but because the soundness claim is unverified by anyone
  outside the project** — and for a credential system, "trust us, we audited ourselves" is not
  an acceptable basis for production reliance. The severity reflects the *assurance* gap, which
  is exactly what an external auditor closes.
- **CR-G7 (MPC) is HIGH for any MPC reliance** but the honest framing is "explicitly deferred,
  no guarantee claimed" — there is no *overclaim* to correct, only a deferral to keep visible.
- The privacy residuals (CR-G6) are **MEDIUM** because they are linkability/binding gaps in a
  system whose *soundness* (the load-bearing property) is separately argued — a privacy leak is
  serious for a privacy-preserving system but does not let a forger pass a false result. <!-- privacy-claims-allow: descriptive of the system class under analysis (not a claim of an achieved property); sq-toze.35 -->

> **Auditor note.** If any future framework (e.g. CDMC, privacy) assigns the ZK/MPC estate a
> maturity score implying it provides a guarantee it disclaims, or cites the internal re-audit
> as external certification, that is a high-severity cross-framework finding — flag it against
> this register's HEADLINE. The most load-bearing honesty item in the repo is the documented
> posture that the ZK verifier is **remediated but NOT externally audited** — no production
> guarantee until the external cryptographer sign-off (`sq-qhy4`) completes; this register
> exists to keep that un-laundered. [OPUS-4.8]
